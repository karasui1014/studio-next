# ローカル開発用。.env.local としてコピーして使う（.env.local はコミットしない）。

# 限定コンテンツ配信API（worker/api）の場所。
# 未設定だと限定コンテンツは一切取得されず、ロック表示のままになる
# （設定漏れで本文が漏れる、という向きには倒れない）。
#   ローカル : http://localhost:8788
#   本番     : https://studio-next-api.<サブドメイン>.workers.dev
VITE_API_BASE=http://localhost:8788
