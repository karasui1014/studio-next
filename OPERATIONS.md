# AI音楽部 Studio — 運営マニュアル

**このファイルは、カラスイさんが日常運営で見るための手順書です。**

開発の詳しい話は `PROJECT_SPEC.md` にあります。こちらは「困ったときに開く本」です。
上から順に読む必要はありません。**いま起きていることの見出しを探して、そこだけ読んでください。**

| | |
|---|---|
| Studioを開く | https://karasui1014.github.io/studio-next/ |
| 作業する場所 | `~/AI音楽部 Studio Next`（ターミナルでここに移動してからコマンドを打つ） |
| 会員の台帳 | `~/軍配/ledgers/LICENSES.md`（このMacの中だけ。ネットには上げていません） |

> ⚠️ **コマンドは、必ず最初にこの1行を実行してから打ってください。**
> 作業する場所に移動するための行です。
>
> ```bash
> cd ~/"AI音楽部 Studio Next"
> ```

---

## いちばん大事なこと（3つだけ）

1. **キーは1人に1本です。** 同じキーを2人に渡さないでください。
   渡してしまうと、片方だけを止めることができなくなります。
2. **キーをYouTubeの限定投稿やDiscordに書かないでください。** 全員が同じキーを見てしまいます。
   渡すのは**LINEのトークで、その人にだけ**です。
3. **キーが表示されるのは発行した一度きりです。** 画面に出たらすぐコピーして送ってください。
   忘れても後から見る方法はありません（新しく発行し直すことになります）。

---

## プランの早見表

| Studioでの呼び方 | YouTubeでの商品名 | 月額 | 何が使えるか |
|---|---|---|---|
| 無料 | （加入なし） | ¥0 | ニュース／Picks冒頭／AIツール図鑑／スタイルプロンプト工房／書き出し |
| Studio Premium | AI音楽部 Studio Premium | ¥1,290 | 制作ツール一式（歌詞レビュー・MVアイデア・AIプロデューサー・プロンプト工房・曲の管理）＋絵コンテ／ギターコードTAB／マスタリング |
| Studio Creator | AI音楽部 Studio Creator | ¥3,490 | **Premiumのすべて** ＋ Seedance Batch Studio ＋ シーダンス2.5 プロンプト工房 |
| Studio Master | AI音楽部Studio Master | ¥5,600 | **Creatorのすべて** ＋ Discordコミュニティ・作品添削・レビュー・限定ライブ・優先サポート |

**上のプランは下のプランを全部含みます。** Masterの人はCreatorの機能も全部使えます。

その他のYouTubeメンバーシップ（**Studioの権限は付きません**）

- `Ai大好き部`（¥690）… 応援用の段です
- `「企業様向け」Aiで曲作りしたい部`（¥35,000）… 企業向けの相談窓口という別商品です。
  **金額が高くてもMaster権限は付けません。**

> 💡 **Masterは「ツールが増えるプラン」ではありません。**
> カラスイさんが直接関わる（添削・レビュー・ライブ・コミュニティ）ことが価値です。
> だから**30名までしか受けられません**。ツールを増やして差をつけないでください。

---

## 会員が入ったとき（Premium／Creator／Master 共通）

やることは3プランとも同じです。**違うのは、発行コマンドに書く単語だけ**です。

### 手順

**1. 公式LINEに「利用申請」が届く**

YouTubeのメンバー限定投稿から、公式LINEに来てもらう流れになっています。

**2. YouTube Studio で、その人が本当に会員か確認する**

YouTube Studio →「収益受け取り」→「メンバーシップ」で会員一覧を開き、
LINEで名乗っているチャンネル名があるか、**どの段に入っているか**を目で確認します。

段の名前と発行するプランの対応が不安なときは、これで確認できます。

```bash
node scripts/membership-levels.mjs --list
```

段の名前をそのまま入れて調べることもできます。

```bash
node scripts/membership-levels.mjs "AI音楽部 Studio Creator"
```

> ℹ️ **段の名前を変えても、認証が止まることはありません**（2026-08-19に確認）。
> このコマンドは**カラスイさんが確認するための道具**で、権限そのものは
> 「どのプランのキーを発行したか」だけで決まります。段名は認証に使っていません。
>
> ただし改名したら、**開発側に連絡してください。** 直さないと次の2つが起きます。
> ① このコマンドが「未知の段名です」と出て、確認の役に立たなくなる
> ② **プラン画面に、YouTubeに存在しない段名が表示される**（加入したい人が探せなくなる）
>
> 詳しくは下の「YouTubeの段名について」を見てください。

**3. キーを発行する**

入っていた段に合わせて、次のどれかを実行します。
`--name` には**YouTubeの表示名**を入れてください（後で「誰に渡したか」を追うために使います）。

Premiumの人:

```bash
node scripts/issue-license-key.mjs premium --name "YouTube表示名" --prod
```

Creatorの人:

```bash
node scripts/issue-license-key.mjs creator --name "YouTube表示名" --prod
```

Masterの人:

```bash
node scripts/issue-license-key.mjs master --name "YouTube表示名" --prod
```

実行すると、画面にこういうものが出ます。

```
=== C-002 を発行しました（本番）===
  申請者 : YouTube表示名
  プラン : creator
  🔑 XXXXX-XXXXX-XXXXX-XXXXX      ← これをコピーして送る
```

- `C-002` のような番号が**連番**です。台帳に自動で記録されます。
- 🔑 の行が**キー本体**です。**この場で控えてください。二度と表示されません。**

**4. LINEのトークで、その人にだけキーを送る**

送る文面の例です。

> お待たせしました。Studioのキーをお送りします。
>
> XXXXX-XXXXX-XXXXX-XXXXX
>
> Studioを開いて「プラン」の画面を出し、
> 「メンバーシップのキーを入れる」の欄に貼り付けて「適用する」を押してください。
> https://karasui1014.github.io/studio-next/
>
> ※このキーはあなた専用です。他の方には渡さないでください。

**5. Masterの人だけ、Discordの招待も送る**

→ 下の「Discordへ案内するとき」を見てください。

---

## ライセンスキーを発行するとき（コマンドだけ知りたい場合）

```bash
node scripts/issue-license-key.mjs <premium|creator|master> --name "YouTube表示名" --prod
```

- `--prod` を**付け忘れると本番に反映されません**（手元だけの練習用になります）。
- プラン変更のときは、あとから見て分かるようにメモを付けられます。

```bash
node scripts/issue-license-key.mjs creator --name "YouTube表示名" --note "PremiumのP-007から変更" --prod
```

### いま何人に発行しているか見る

```bash
node scripts/issue-license-key.mjs --status
```

```
  Premium : 有効 1名（失効 5 / 累計 6）
  Creator : 有効 0名（失効 1 / 累計 1）
  Master  : 有効 2名（失効 7 / 累計 9）
            上限 30名 — 残り 28枠
```

**Masterの残り枠はここで確認します。** 30名に達すると、コマンドが自動で止まります
（うっかり31人目を発行してしまうことはありません）。

---

## 退会したとき

**キーは自動では止まりません。手で止める必要があります。**

**1. 台帳でその人の連番を調べる**

```bash
open ~/軍配/ledgers/LICENSES.md
```

名前で探して、左端の `P-007` のような連番を控えます。

**2. 失効させる**

```bash
node scripts/revoke-license-key.mjs P-007 --note "退会のため失効" --prod
```

**3. Masterだった人なら、Discordからも外す**

Discordは自動で連動しません。手で外してください。

> ✅ **失効は、次にその人がStudioを開いた時点で効きます**（2026-08-19改善）。
> 以前は「キーは使えなくなるが、今開いている画面は最大30日そのまま」でしたが、
> Studioは起動のたびにサーバーへ権限を確認しに行くため、
> **失効させたあと最初の起動で無料プランに戻ります。**
>
> ただし、**その人がStudioを開きっぱなしにしている間は切り替わりません**
> （画面を開き直すか再読み込みしたときに反映されます）。
>
> 📌 **2026-09-18ごろまでの経過措置**：この改善より前にキーを入れた人のトークンには
> 失効を確認するための番号が入っていません。その人たちだけは従来どおり最大30日残ります。
> 全員のトークンが入れ替わる2026-09-18以降は、経過措置は自然に終わります。
> 早く確実に止めたい場合は、本人に「プラン画面で『無料に戻す』を押してください」と
> 伝えるのが最も確実です。

---

## プラン変更したとき

**古いキーのプランを書き換えることはできません。**
**「新しいキーを出す」→「古いキーを止める」**の順で行います。

例：Premium（P-007）の人が Creator に上がった場合。

**1. 新しいキーを発行する**

```bash
node scripts/issue-license-key.mjs creator --name "YouTube表示名" --note "PremiumのP-007から変更" --prod
```

**2. LINEで新しいキーを送る**

> プラン変更ありがとうございます。新しいキーをお送りします。
> 「プラン」画面でこちらを入力してください。前のキーは使えなくなります。

**3. 本人が入力したのを確認してから、古いキーを止める**

```bash
node scripts/revoke-license-key.mjs P-007 --note "CreatorのC-002へ変更のため失効" --prod
```

> 💡 **順番が大事です。** 先に古いキーを止めると、新しいキーを入れるまでの間、
> その人はStudioを使えなくなります。

ダウングレード（Master → Premium など）も、まったく同じ手順です。
Masterから下がった人は、**Discordからも外してください。**

---

## キーを失効するとき

```bash
node scripts/revoke-license-key.mjs <連番> --note "理由" --prod
```

失効させる理由の例：退会した／プランを変えた／キーが人目に触れた／間違えて発行した。

**キーが人目に触れたとき（スクリーンショットに写った、投稿に貼ってしまった等）は、
実害があったかどうかに関わらず、すぐに失効させて新しいキーを発行してください。**

失効させても台帳の行は消えません。「失効」と記録が残るだけです。
誰にいつ渡して、いつ止めたかを後から追えるようにするためです。

---

## Discordへ案内するとき

**Masterの人だけ**です。Creator以下の人には案内しないでください。

- Discordの招待URLは**このリポジトリにも、Studioの画面にも書いていません**。
  URLを知った人が誰でも入れてしまうと、30名という上限が崩れるためです。
- 招待は**LINEのトークで、キーと一緒に手で送ってください**。
- 招待URLの控えは `~/軍配/house-rules.md`（このMacの中だけ）にあります。

Masterの人がプラン画面を開くと「Discordコミュニティの招待は、公式LINEからお送りしています」
という案内が出ます。届いていない人はLINEに連絡してきます。

---

## 退会の確認（棚卸し）

退会はYouTube側で起きるだけで、**Studioには自動で伝わりません。**
ここだけは人が見る必要があります。

### いつ確認するか

| タイミング | やること |
|---|---|
| **毎月1日**（定期・5分） | 下の手順1〜5をひととおり |
| **返金した／誤ってキーを出した／キーが人目に触れた** | **その場ですぐ失効**。月初を待たない |
| LINEでプラン変更の連絡が来たとき | 「プラン変更したとき」の手順 |

> **なぜ月1回で足りるのか。**
> いまの有効な会員は3名で、YouTubeのメンバーシップは**解約しても支払い済みの期間は続く**
> のが普通です。つまり「解約した直後にすぐ止める」必要はなく、
> 月初にまとめて確認すれば実害はほとんどありません。
>
> **会員が10名を超えたら、月2回（1日と15日）に増やしてください。**
> 見落としたときの人数が増え、1回あたりの確認も長くなるためです。
>
> ⚠️ 返金・誤発行・キーの流出だけは例外です。**月初を待たず、その場で失効させてください。**

### 手順

**1. YouTube Studio で今の会員一覧を開く**

YouTube Studio → 左メニューの **「収益受け取り」** → **「メンバーシップ」** タブ。
ここに、いま課金が続いている人が一覧で出ます。
**表示名**と**入っている段の名前**の2つを見てください。

**2. Studio側の台帳を開く**

```bash
open ~/軍配/ledgers/LICENSES.md
```

「状態」の列が **有効** になっている行が、いまStudioを使える人です。

**3. 突き合わせる**

見るのは次の2つだけです。

| 見つけたもの | どうするか |
|---|---|
| 台帳が「有効」なのに、YouTubeの一覧に**居ない** | → **退会**。手順4へ |
| 台帳と違う段に入っている | → **プラン変更**。「プラン変更したとき」の手順へ |

**4. 退会していた人のキーを失効させる**

```bash
node scripts/revoke-license-key.mjs <連番> --note "棚卸しで退会を確認" --prod
```

`<連番>` は台帳の左端（`P-007` など）です。**`--prod` を忘れないでください。**

成功すると、こう出ます。

```
✅ P-007 を失効させました
   台帳: 状態を「失効」に更新しました
```

**5. 失効できたことを確認する**

次の2つを見れば十分です。

① 台帳のその行が **失効** になっていること

```bash
open ~/軍配/ledgers/LICENSES.md
```

② 有効な人数が1人減っていること

```bash
node scripts/issue-license-key.mjs --status
```

これで、その人は**次にStudioを開いた時点で無料プランに戻ります。**

> さらに念を入れたい場合は、その人の連番でセッションの期限を確認できます
> （経過措置中の人がいるかを見るためのものです）。
>
> ```bash
> node scripts/check-license-expiry.mjs <連番> --prod
> ```

**6. ついでにMasterの残り枠を見ておく**

```bash
node scripts/issue-license-key.mjs --status
```

### 将来やるかもしれないこと（いまはやりません）

- YouTubeの会員CSVを書き出して、台帳と自動で突き合わせる
- 会員一覧をAPIで取得して自動照合する（**現在は使えません**。→ 下の「YouTubeの段名について」）

どちらも**新しい外部サービスや課金の仕組みは増やさずに**できる範囲の話です。
会員が増えて手作業がつらくなったら相談してください。

---

## YouTubeの段名について

### 名前を変えても認証は止まりません

段の名前（`AI音楽部 Studio Creator` など）を**認証には使っていません**。
権限は「運営者がどのプランのキーを発行したか」だけで決まります。
なので、YouTube側で自由に改名して構いません。

### それでも改名したら連絡が必要な理由

| 直さないと起きること | 深刻さ |
|---|---|
| プラン画面に、YouTubeに存在しない段名が表示される | 🟡 加入したい人が段を探せない |
| `membership-levels.mjs --list` が「未知の段名」と出る | 🟢 確認の道具が使えないだけ |

**改名したら、新しい名前をそのまま開発側に伝えてください。** 2箇所を直します
（ずれていないかはテストで自動的に見張っています）。

### なぜIDで管理していないのか

本来は、名前ではなく**変わらないID**で管理するのが安全です。
ただし現在は**IDを取得する手段がありません**。

- YouTubeのAPI（`members.list` / `membershipsLevels.list`）は
  **403エラーで使えません**。使うにはYouTube側の担当者（Partner Manager）が必要で、
  このチャンネルには付いていません
- 会員CSVにIDが含まれるかは**未確認**です

**分からないIDを推測して書くと、全員が権限を失います。** そのため、
IDが取得できると確認できるまでは名前で管理し、旧名も残して照合できるようにしています。

---

## 表示が古いままのとき

**Studio Next にはアプリを端末に保存する仕組み（Service Worker）は入っていません。**
そのため「アプリが古いまま固まる」ことは起きません。

更新してから最大**10分**は、ブラウザが前のページを覚えていることがあります
（GitHub Pages の設定によるもので、10分経てば自然に新しくなります）。

すぐ新しくしたいときは、スーパーリロードを試してください。

- Mac … `Command + Shift + R`
- Windows … `Ctrl + Shift + R`

> ⚠️ **V1（旧Studio／`~/AI Music Studio`）は別です。**
> あちらにはService Workerが入っており、「更新したのに古い画面が残る」問題が
> 実際に起きています。直すには**あちらのリポジトリでの作業**が必要です。

---

## トラブルが起きたとき

### 「キーを入れたのに『確認できませんでした』と出る」

赤い字で「確認できませんでした」と出る場合、次のどれかです。

| 考えられること | 確かめ方 |
|---|---|
| キーの打ち間違い・コピーもれ | もう一度、丸ごとコピーして貼り直してもらう |
| すでに失効させたキー | 台帳でその連番が「失効」になっていないか見る |
| そもそも発行していない | 台帳にその人の行があるか見る |
| 短時間に何度も試した | 1分ほど待ってから、もう一度試してもらう |

台帳を開いて確認します。

```bash
open ~/軍配/ledgers/LICENSES.md
```

それでも分からないときは、**新しいキーを発行して渡し、古い方を失効させる**のが
いちばん早い解決です（原因追及より復旧を優先してください）。

### 「サイトが真っ白になる」

まずスーパーリロードを試してもらいます（Mac `Command + Shift + R` ／ Windows `Ctrl + Shift + R`）。
それでも直らないときは、ブラウザの履歴・キャッシュを消して開き直してもらってください。

「更新したのに古いまま」については、上の「表示が古いままのとき」を見てください。

### 「Studio全体が動かない」

サーバー側が生きているかを確認します。

```bash
curl -s https://studio-next-api.karasui1969.workers.dev/api/health
```

こういう返事が来れば、サーバーは正常です（この場合は個別の問題を疑ってください）。

```
{"ok":true,"counts":{...},"contentLoaded":true,...}
```

返事が来ない・エラーになる場合はサーバー側の問題です。開発側に連絡してください。

### 「サイトの更新が反映されない」

サイトはGitHubに変更を送ると自動で公開される仕組みです（GitHub Actions）。
公開作業が失敗していないかを確認します。

```bash
gh run list --limit 5
```

`completed  success` なら公開は成功しています。`failure` があれば開発側に連絡してください。

### 誰かに「キーを教えて」と言われた

**渡さないでください。** キーは1人1本です。
その人が会員なら、**その人専用のキーを新しく発行して**渡してください。

---

## やってはいけないこと

- ❌ **キーをYouTubeの限定投稿・Discord・SNSに書く**（全員が同じキーを見てしまいます）
- ❌ **同じキーを複数人に渡す**（片方だけ止められなくなります）
- ❌ **Discordの招待URLをStudioの画面やコードに書く**（30名の上限が崩れます）
- ❌ **35,000円の企業様向けプランの人にMasterキーを出す**（別商品です。金額は関係ありません）
- ❌ **`--prod` を付け忘れて発行する**（本番に反映されず「使えない」と言われます）
- ❌ **台帳（`LICENSES.md`）を手で書き換える**（表の形が崩れるとコマンドが読めなくなります）

---

## 用語の言いかえ

開発側とやりとりするときに出てくる言葉です。

| 言葉 | 意味 |
|---|---|
| ライセンスキー | Studioで権限を付けるための、その人専用の合言葉 |
| 連番 | `P-007` `C-002` `M-011` のような通し番号。**P＝Premium、C＝Creator、M＝Master** |
| 失効（しっこう） | そのキーを使えなくすること。台帳の行は消えず「失効」と残る |
| 台帳 | 誰にどのキーを渡したかの記録。このMacの中だけにあります |
| ロール（role） | 権限のこと。`free` / `premium` / `creator` / `master` の4つ |
| 棚卸し | 月に1回、YouTubeの会員一覧と台帳を見比べる作業 |

---

## 開発側に依頼するときのメモ

次のことは、カラスイさんの操作では変えられません。開発側への依頼が必要です。

- YouTubeの段の名前を変えた（→ 照合表の更新が必要。**放置すると新規の人が全員止まります**）
- プランの価格を変えたい
- プランを増やしたい・減らしたい
- Studioの画面の文言を変えたい
- 「退会してから権限が止まるまで最大30日」を短くしたい（改善案は比較済み・未着手）
