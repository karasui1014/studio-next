# AI音楽部 Studio — プロジェクト仕様書

**このファイルが唯一の正（Single Source of Truth）です。**
実装を変更したら、必ずこのファイルも同じコミットで更新してください。
ここに書かれていないことは「決まっていない」と判断してください。

| | |
|---|---|
| 最終更新 | 2026-08-02 |
| バージョン | Version 2（開発中・未公開） |
| リポジトリ名 | `studio-next` |
| 公開予定URL | `https://karasui1014.github.io/studio-next/` |
| ローカル起動 | `npm run dev` → http://localhost:5180 |

### 記号の意味

このファイル全体で、次の3つを区別しています。

| 記号 | 意味 |
|---|---|
| ✅ **決定** | 合意済み。実装済み、または実装方針が固まっている |
| 🚧 **未実装** | 決定済みだが、まだコードになっていない |
| ❓ **検討中** | まだ決まっていない。勝手に決めずに確認すること |

---

## 0. 新しく開発を引き継ぐ人へ

### まず読む順番

1. このファイル（全体像）
2. `docs/設計方針.md`（なぜそう決めたかの経緯）
3. `README.md`（動かし方・公開手順）

### 最初にやること

```bash
cd "~/AI音楽部 Studio Next"
npm install
npm run dev     # http://localhost:5180
npm test        # 141件通れば健全

# 限定コンテンツを扱うなら（Premium/Master の表示を確認したいとき）
cd worker/api && npm install && cp .dev.vars.example .dev.vars && npm run dev   # :8788
```

### 絶対に守ること

1. **既存Studio（Version 1、`~/AI Music Studio`）を絶対に変更しない。**
   これは別プロジェクトです。V1は本番稼働中なので触りません。
2. **ユーザーの制作データを外部に送らない。** 制作データは端末内（localStorage）のみです。
   Worker（`worker/api/`）は**権限判定と限定コンテンツ配信だけ**を担い、制作データは扱いません。
3. **データの書き出しは無料のまま維持する。** ここを有料化するとデータの人質化になります。
4. **権限は価格ではなく Role で判定する。** 画面側は必ず `useRole()` を通します。
5. **`public/` に置いたものは公開物。** Premium限定の本文を置いてはいけません。
   ログインの有無に関係なくURLで読めます（→ §5-A）。
6. **権限判定を画面側に書かない。** role を見て本文を出し分けるコードを書いた時点で、
   本文がフロントに存在することになります。判定は `resolveMembership()` だけです。

### 迷ったときの判断基準

> **Studioは「読む場所」ではなく「作る場所」。**
> ユーザーが開いた瞬間に思ってほしいのは「今日は何を作ろう」であって、
> 「今日はどんなニュースがあるだろう」ではない。

機能を足すか迷ったら、これに沿うかで判断してください。

---

## 1. プロジェクト概要

### 正式名称

| 項目 | 値 |
|---|---|
| サービス名 | **AI音楽部 Studio** |
| サブタイトル | **AI音楽クリエイターのホーム** |

✅ **決定**：この2つは全画面・全ドキュメントで統一します。表記ゆれを作らないこと。

### コンセプト

AI音楽クリエイターが**毎日最初に開くホーム画面**。

ニュースサイトではありません。毎日開いて制作したくなるStudioです。
ニュースは「毎日来る理由」であって「居る理由」ではありません。
居る理由は、自分の制作がそこにあることです。

### ターゲット

| 層 | 状態 | Studioでの体験 |
|---|---|---|
| これから始める人 | AI音楽に興味があるが未着手 | 無料で情報とツールに触れる |
| 日常的に作る人 | 週に何曲か作っている | Premiumで制作環境を上げる |
| 仕事にしたい人 | 収益化・作品性を上げたい | Masterで人に見てもらう |

### 成功指標

✅ **決定**：訪問数ではなく **連続利用日数（継続してStudioを開いた日数）**。
この指標に効かない機能は後回しにします。

### 4つの場所の役割分担

✅ **決定**：それぞれの役割を混ぜないこと。

| 場所 | 役割 |
|---|---|
| YouTube | **学ぶ**（出会いの入口） |
| Substack | **記事を読む** |
| Discord（AI音楽部 Lab） | **交流する** |
| **Studio** | **毎日制作する** |

理想の流れ：

```
YouTubeで新しいAIツールを紹介
    ↓
詳しい検証や考察はSubstackに書く
    ↓
Studioに「カラスイ Picks」として自動表示される
    ↓
Premium会員はStudio内で制作メモや関連プロンプトにアクセス
```

一つ記事を書くたびに、YouTube・Substack・Studioの3つが同時に育つ設計です。

---

## 2. サービス全体構成

### 画面一覧と実装状況

| 画面 | ルート | 実装 | 実装ファイル |
|---|---|---|---|
| ホーム | `/` | ✅ 完了 | `src/features/home/HomePage.tsx` |
| AI音楽ニュース | `/news` | ✅ 完了 | `src/features/news/NewsPage.tsx` |
| カラスイ Picks | `/picks` | ✅ 完了 | `src/features/picks/PicksPage.tsx` |
| AIツール図鑑 | `/tools-guide` | ✅ 完了 | `src/features/tools-guide/ToolsGuidePage.tsx` |
| 制作ツール一覧 | `/tools` | ✅ 完了 | `src/features/tools/ToolsPage.tsx` |
| ├ 歌詞レビュー | `/tools/lyrics-review` | ✅ 完了 | `src/features/tools/lyrics-review/` |
| ├ MVアイデア | `/tools/mv-idea` | ✅ 完了 | `src/features/tools/mv-idea/` |
| ├ AIプロデューサー | `/tools/ai-producer` | ✅ 完了 | `src/features/tools/ai-producer/` |
| └ ストーリーボード | `/storyboard` | ✅ 完了 | MVアイデアと同じ画面 |
| プロンプト工房 | `/prompts` | ✅ 完了 | `src/features/tools/prompt-dex/` |
| 曲一覧 | `/songs` | ✅ 完了 | `src/features/songs/SongsPage.tsx` |
| 曲詳細 | `/songs/:songId` | ✅ 完了 | `src/features/songs/SongDetailPage.tsx` |
| データ管理 | `/data` | ✅ 完了 | `src/features/data/DataPage.tsx` |
| プラン | `/plans` | ✅ 完了 | `src/features/plans/PlansPage.tsx` |
| AI秘書 | `/secretary` | ✅ 完了 | `src/features/secretary/`（ロジックは `src/core/secretary/`） |
| マイページ | — | 🚧 未実装 | 下記参照 |

**AI秘書について** ✅ **2026-08-10 完了**（V1から移植）
名前・一人称・話し方・応援スタイル・口癖・画像を利用者が決める「相棒」。
**AIやAPIには一切問い合わせない**——文章は `core/secretary/message.ts` の
ルールだけで組み立てる。曲データのどこで手が止まっているかが分かれば、
次の一歩は機械的に決まるため、それで十分に役に立つというV1からの判断。

- **権限**：Premium以上（`PremiumGate`）。話す内容の元が曲データ（Premium機能）のため
- **ホームのカード**：有料プランのときだけ描画する。無料の人に出しても
  「今日は何から作りますか？」しか言えず、押した先がロック画面になるため
- **レベル**：`status` が `published` の曲数だけで決まる（V1は `completedAt` を見ていたが、
  V2の曲データに完了日時が無いため対応させた）。作りかけは数えない
- **保存先**：設定と節目は localStorage、画像は IndexedDB。どちらも端末内のみ
- **書き出し**：`/data` の書き出しに秘書の設定・節目・画像（data URL）を含める。
  端末を変えて秘書が初期化されるのでは「持ち出せる」と言えないため

**マイページについて** ❓ **検討中**
現在は「マイスタジオ」というナビゲーショングループ（AI秘書・データ管理・プラン）で
代用しています。独立した「マイページ」を作るかどうかは未決定です。
作る場合、何を載せるか（制作統計・連続日数・保存データの概要など）も未決定。

### ナビゲーション

✅ **決定**：機能名ではなく**目的**でグループ分けします。

```
ホーム

知る
  AI音楽ニュース
  カラスイ Picks
  AIツール図鑑

つくる
  曲一覧
  プロンプト工房
  ストーリーボード
  制作ツール

マイスタジオ
  AI秘書 / データ管理 / プラン
```

- **PC**：左サイドバー（グループ見出し付き）— `src/app/Sidebar.tsx`
- **スマホ**：下部タブ5つ（ホーム／ニュース／つくる／学ぶ／マイ）— `src/app/TabBar.tsx`

スマホを下部タブにしたのは、毎日開くサービスは片手で操作できる必要があるためです。

---

## 3. ホーム画面仕様

### 表示順（✅ 決定・変更禁止）

実装：`src/features/home/HomePage.tsx`

| # | ブロック | 目的 | コンポーネント |
|---|---|---|---|
| ① | **曲を作る** | 制作の入口。**いちばん大きく別格に** | `CreateHero.tsx` |
| ② | 歌詞を書く | 制作の入口 | `CreateHero.tsx` |
| ③ | MVを作る | 制作の入口 | `CreateHero.tsx` |
| ④ | プロンプトを作る | 制作の入口 | `CreateHero.tsx` |
| ⑤ | **続きから** | 前回のつづきへ戻る | `ResumeSection.tsx` |
| ⑥ | 今日の重要ニュース（**3件だけ**） | 情報収集。主役ではない | `HomePage.tsx` 内 |
| ⑦ | **🔥 今日のカラスイ Picks（1件だけ）** | 制作者目線の解説 | `TodayPick.tsx` |
| ⑧ | おすすめプロンプト | ふたたび制作へ | `sections.tsx` の `PromptCard` |
| ⑨ | 制作ツール | 目的が決まっている人の近道 | `HomePage.tsx` 内 |
| ⑩ | 今日試すAIツール | 明日また来る理由 | `DailyTip.tsx` |

末尾に Substack の登録案内カードを1枚だけ置いています（`sections.tsx` の `SubstackCard`）。

**設計意図**：「制作 → 情報収集 → 学習 → ふたたび制作」の流れになるようにしています。

### ①〜④ 制作タイルの仕様

✅ **決定**：4つを同格にしない。「曲を作る」だけ別格に大きくします。
利用頻度が最も高くなる想定のためです。

| タイル | 遷移先 | 色 | 見た目 |
|---|---|---|---|
| 曲を作る | `/songs` | primary→suno グラデーション | **塗りつぶし・白文字・影あり・大** |
| 歌詞を書く | `/tools` | primary（枠線＋淡い背景） | 小 |
| MVを作る | `/storyboard` | mv（枠線＋淡い背景） | 小 |
| プロンプト | `/prompts` | premium（枠線＋淡い背景） | 小 |

- 色は用途ごとに固定します（毎日見ても迷わないため）
- 挨拶文は時間帯で変わります（朝／昼／夜）
- 右上に連続利用日数を小さく表示します

### ⑤ 続きから の仕様

✅ **決定**：履歴がまだ無い人には**セクションごと表示しません**（初回は入口だけを見せる）。

表示するもの：

1. **制作中の曲**（最終更新が最も新しい1曲）— 大きめのカードで「続きを作る」ボタン付き
2. **最近ひらいたもの**（最大8件・横スクロール）
3. **お気に入り**（最大8件・横スクロール）

記録するのは「何を開いたか」だけで、**作品の中身は保存しません**。

### ⑥ ニュースの見せ方

✅ **決定**：カードを大きくしない。**枠1つの中に3行**という控えめな見た目にします。

同じ3件でも、枠線1本でまとめて行の高さを下げるだけで「主役ではない」ことが伝わります。
情報量を減らすのではなく、**視覚的な重みだけを下げる**のがポイントです。

見出し：`今日の重要ニュース` ／ 補足：`3件・3分で読めます` ／ 右上：`もっと見る →`

### ⑦ カラスイ Picks の見せ方

✅ **決定**：**ホームは1本だけ**。一覧はPicksページに置きます（ホームで迷わせないため）。

```
🔥 今日のカラスイ Picks    今日の注目ニュースを制作者目線で解説    もっと見る →
┌────────────────────────────────────┐
│ [アイキャッチ]  今日の注目テーマ  2026年6月22日
│                大きなタイトル
│                冒頭300文字…
│                ［ 続きを読む → ］
│ ─── カラスイの制作メモ（Premium） ───
└────────────────────────────────────┘
```

ボタン文言は **「続きを読む」**。サービス名（Substack）はボタンに出しません。

### ⑩ 今日試すAIツール の仕様

✅ **決定**：`public/content/home.json` の `daily` から**1日1件だけ**選んで表示します。

選び方（`src/features/home/DailyTip.tsx` の `pickDailyTip()`）：

1. `date` が今日と一致する項目があれば、それを最優先で表示
2. 無ければ、`date` を持たない項目を**日替わりで巡回**

**この仕組みが重要な理由**：毎日書かなくても毎日変わるので、運営が止まりません。
Premiumの価値を「毎週コンテンツを配ること」に置かない方針と同じ考え方です。

現在は Suno Persona / Extend / Cover / Lyrics改善 / ステム分離 / Seedance / DomoAI など
8件が登録されており、何もしなくても8日サイクルで回ります。

---

## 4. プラン設計

### 3つのプラン

| | 無料 | Studio Premium | Studio Master |
|---|---|---|---|
| 種別 | ツール | **制作環境** | **学習環境（伴走）** |
| 目的 | まず触ってみる | 制作環境をアップグレードする | 本気で学び、成長する |
| 対象 | これから始める人 | 日常的に制作するクリエイター | 見てもらいながら制作力を伸ばしたい人 |
| スケール | 無限 | 無限 | **30名まで** |

✅ **決定**：Premium と Master は**上下関係ではなく別の目的**です。
「Premiumで足りない人がMaster」ではなく、
**「ひとりで作る人がPremium、見てもらって伸ばす人がMaster」**。

この並列関係を崩さないでください。上位互換として見せると、価格差が
「機能量の差」に見えてしまい、必ず割高に感じられます。

### 価格（✅ 決定・2026-08-02、2026-08-12にStudio直販を廃止）

**加入経路はYouTubeメンバーシップのみです。Studio直販は行いません。**

以前は「Studio直販」という別価格（Premium 980円・Master 5,600円）を
併記していましたが、実際にはStudioで直接購入できる手段（決済導線）は
一度も実装されておらず、表示だけが存在する状態でした。加えて実装面でも、
実際に発行されるセッションの `source` は常に `license`（＝YouTube会員確認を
経て発行）で、`'studio'` という区分値がUIに現れることは構造的にありませんでした。
存在しない経路を表示し続けると混乱のもとになるため、`STUDIO_PRICE` 定数・
`RoleSource` の `'studio'` を削除し、**価格はYouTubeメンバーシップの1本のみ**にした
（実装：`src/core/entitlement/role.ts` の `YOUTUBE_TIERS`）。

**YouTubeメンバーシップ**（実装：`src/core/entitlement/role.ts` の `YOUTUBE_TIERS`）

| 段（YouTube側の商品名） | 価格 | 付与されるRole | Studio内の表示名 |
|---|---|---|---|
| Ai大好き部 | 690円 / 月 | **なし**（応援用の段。Studioの権限は付かない） | — |
| AI音楽部 Studio Premium | 1,290円 / 月 | `premium` | Studio Premium |
| AI音楽部 Studio Creator | 3,490円 / 月 | `creator` | Studio Creator |
| AI音楽部Studio Master | 5,600円 / 月 | `master` | Studio Master |
| 「企業様向け」Aiで曲作りしたい部 | 35,000円 / 月 | **なし**（→ 下記） | — |

⚠️ **段名はYouTube Studioの実画面の表記をそのまま写すこと**（2026-08-19確認）。
`Ai大好き部`（AIではない）、`AI音楽部Studio Master`（部とStudioの間に空白が無い）など
段ごとに不揃いだが、**見た目を揃えるために書き換えてはいけない**。
実物とずれた名前は、照合時に静かに全員 `free` へ倒れる原因になる。

> **YouTubeの商品名とStudioのプラン名は別物です（✅ 2026-08-19決定）。**
> Studio内では「AI音楽部」を付けず `Studio Creator` と表示し、
> YouTubeの加入案内など**メンバーシップの正式名称が要る場所でだけ**
> `AI音楽部 Studio Creator` と表示します。
> 実装は `role.ts` の `ROLE_LABEL`（Studio名）と `YOUTUBE_TIERS[].youtubeName`（YouTube名）に
> 分けてあり、画面側に直接書きません。
>
> Premium / Master の段名は 2026-08-19 に「AI音楽部 Studio ○○」へ改名されました。
> 旧名（「AiでMVを作りたい部」など）は `scripts/membership-levels.mjs` の `aliases` に
> 残してあります（改名前に申請した人の控えと突き合わせるため）。

#### ⚠️ 35,000円「企業様向け」プランの扱い（✅ 2026-08-09 決定・厳守）

> **「企業様向け」Aiで曲作りしたい部（35,000円）は、企業向け相談窓口という別商品であり、
> AI音楽部 Studioの権限体系とは無関係。Premium / Master権限は付与せず、
> ライセンスキー発行対象にも含めない。**

具体的には、次をすべて行いません。

- Studio Premium 権限の付与
- Studio Master 権限の付与
- ライセンスキーの発行
- Studio利用申請の対象に含めること
- Studioのプラン説明に載せること
- このプラン専用のStudio機能を作ること

**価格が高いことを理由にMasterへ自動昇格させてはいけません。**
YouTubeの加入画面では最上位に見えますが、「価格順で最上位＝Master」という
推論は誤りです。Role は**明示的な段名→Role対応**でのみ決めます
（実装：`scripts/membership-levels.mjs` の `role: null`）。

なお現行の `worker/api`（ライセンスキー方式）には価格という概念自体が無く、
Roleは発行時に運営者が決めてKVへ記録した値だけで決まります。
このため自動昇格は構造的に起こりませんが、将来CSV照合や自動発行を導入する際に
再びこの誤りが起きうるため、判断基準としてここに残します。

プラン画面の価格表示は**YOUTUBE_TIERSの価格のみ**を大きく表示します
（Premium 1,290円 / Creator 3,490円 / Master 5,600円）。二重価格ではありません。

Masterの5,600円は現行のYouTube最上位メンバーシップと同額です。
既存メンバーとの公平性のため、この価格は下げません。

### 各プランの中身（✅ 決定）

**無料**
- AI音楽ニュース（全文・日本語訳つき）
- カラスイ Picks（冒頭）
- AIツール図鑑
- 制作ツール（歌詞レビュー・MVアイデア・AIプロデューサー）
- 曲の管理
- プロンプト工房（基本セット）
- **データの書き出し** ← ここは絶対に有料化しない

**Studio Premium**
- Premium限定ツール
- プロンプトライブラリ
- 制作テンプレート
- AI音楽ニュース詳細版
- カラスイ Picks 全文
- 制作履歴
- お気に入り
- 新機能先行公開

**Studio Creator**（✅ 新設・2026-08-18／Premiumのすべて ＋ 映像制作）

位置づけは「機能が増えるプラン」ではなく、
**AI音楽制作から、映像・ビジュアル表現まで制作領域を広げるプラン**です。
対象は「AI音楽から映像制作まで、AIを使った制作の幅を広げたいクリエイター」。

- Studio Premium のすべて
- **Seedance Batch Studio**（外部ツール）
- **シーダンス2.5 プロンプト工房**（外部ツール）

この2つが Creator 限定機能です。2026-08-18 まではMaster限定でしたが、
Creator新設にあたり Creator 以上へ引き下げました
（Masterの差別化は「人が関わる伴走」に一本化する、という整理）。

**Studio Master**（Creatorのすべて ＋ ここから先は人が関わる）

⚠️ **Masterの価値を「さらに多くのAIツールが使えるプラン」にしないこと。**
中心価値は人が関わる伴走・学習環境です。

- 作品添削
- AI音楽レビュー
- Discord Master限定コミュニティ
- Master限定ライブ
- 優先サポート

### Premiumの価値モデル（✅ 決定・重要）

**「毎週コンテンツを配る定期便」ではありません。**
**「プロダクトが進化し続けること」に価値を置きます。**（Cursor / Notion 型）

| | 定期便モデル（**不採用**） | 進化する道具モデル（**採用**） |
|---|---|---|
| 価値の源泉 | 毎週配るコンテンツ | プロダクトそのものが良くなること |
| 継続理由 | 「今週も何か届く」 | 「手放すと制作が遅くなる」 |
| 運営負荷 | 毎週締切がある | 締切がない。積み上げでよい |
| 破綻条件 | 1週休むと解約が始まる | 数か月止まらない限り問題ない |

**なぜ定期便をやめたか**：毎週コンテンツを作る運営は一人では破綻します。
そして破綻した瞬間に「今週は何も来なかった」という解約理由が生まれます。
つまり定期便モデルは、リスクを減らすどころか新しい失敗ポイントを増やしていました。

Premiumで売るのは次の4つです。

1. Studioが便利になり続ける（新機能・改善が毎月入る）
2. 制作時間が短くなる（テンプレート・ワークフロー）
3. AI音楽ニュースが毎日更新される（**すでに全自動**）
4. ツールが増えていく

このうち3つは、すでに自動化されているか通常の開発で自然に積み上がります。
「Premiumのために追加でやる作業」がほぼ発生しないのが、定期便モデルとの決定的な差です。

### Master の人数上限（✅ 決定）

**30名まで。** 上限がないと添削が破綻し、「反応が遅い」という
最悪の解約理由を生みます。UIでは常時「少人数制（30名まで）」と表示します。

### Version 1 ユーザーへの配慮（✅ 決定・実施必須）

制作履歴とお気に入りをPremiumに含めた判断とセットで、次を必ず実施します。

1. **V1利用者がV2へ移行する際、Premiumを1年間無償で付与する**
   （期間は「永年」ではなく「1年間」。その後は運営状況を見て判断）
2. **データの書き出しは無料のまま維持する**

これを守らないと、V1で無料だった機能が有料になったと受け取られます。

---

## 5. 権限設計（Role）

### 原則

✅ **決定**：**権限は価格ではなく Role で管理します。**

```
free < premium < creator < master
```

**上位は下位をすべて含みます（継承型）。**（✅ 2026-08-18 決定）

| Role | 使えるもの |
|---|---|
| `free` | 無料機能のみ |
| `premium` | Premium機能 |
| `creator` | Premium機能 **＋ Creator限定機能** |
| `master` | Premium機能 ＋ Creator機能 ＋ **Master機能** |

Creator限定機能は **Seedance Batch Studio** と **シーダンス2.5 プロンプト工房** の2つです。
Creatorを追加したことで**既存Masterユーザーが失う機能はありません**（Masterはcreatorを包含）。

入手経路が違っても、最終的に同じ Role になります。
**画面側は「いくら払ったか」「どこで買ったか」を一切見ません。**

| 入手経路 | Role |
|---|---|
| YouTube「AI音楽部 Studio Premium」 1,290円 | `premium` |
| YouTube「AI音楽部 Studio Creator」 3,490円 | `creator` |
| YouTube「AI音楽部Studio Master」 5,600円 | `master` |
| YouTube「Ai大好き部」 690円 | （権限なし・`free` のまま） |
| YouTube「「企業様向け」Aiで曲作りしたい部」 35,000円 | （権限なし・`free` のまま。→ §4） |

加入経路はYouTubeメンバーシップのみです（Studio直販は行いません・2026-08-12決定）。

### 実装

**唯一の実装場所：`src/core/entitlement/role.ts`**

```ts
export type Role = 'free' | 'premium' | 'creator' | 'master'
export type RoleSource = 'none' | 'youtube'

// 権限の並び。上下の比較は必ずこれを通す
export const ROLE_RANK: Record<Role, number> = { free: 0, premium: 1, creator: 2, master: 3 }
export function satisfiesRole(actual: Role, required: Role): boolean

// 画面側はこれだけを使う
export function useRole() {
  return { role, source, paid, creator, master, label, can }
}
```

| エクスポート | 用途 |
|---|---|
| `useRole()` | **画面側は必ずこれを使う。** 直接ストアを触らない |
| `useRole().can(required)` | その権限が `required` 以上か。プランが増えても画面は書き換え不要 |
| `ROLE_RANK` / `satisfiesRole()` | 権限の並びと上下比較。**各画面が独自の順序表を持たないこと** |
| `isPaid(role)` | Premium以上か |
| `isCreator(role)` | Creator以上か |
| `isMaster(role)` | Masterか |
| `ROLE_LABEL` | **Studio内のプラン名**（例：Studio Creator）。「AI音楽部」は付けない |
| `YOUTUBE_TIERS[].youtubeName` | **YouTube側の商品名**（例：AI音楽部 Studio Creator）。加入案内でだけ使う |
| `YOUTUBE_TIERS` | YouTubeメンバーシップとRoleの対応（価格の唯一の出典） |
| `youtubeTierFor(role)` | Roleに対応するYouTubeの段を引く |

Worker側にも同じ並びがあります（`worker/api/src/session.ts` の `ROLE_RANK` と `satisfies()`）。
フロントとWorkerで `Role` 型が二重に定義されているため、**プランを増やすときは両方**直すこと。
| `YOUTUBE_JOIN_URL` | 「YouTubeで加入する」ボタンの遷移先（✅ 決定・2026-08-03） |
| `OFFICIAL_LINE_URL` | 公式LINEの「友だち追加」URL（✅ 決定・2026-08-11） |
| ~~`DISCORD_INVITE_URL`~~ | **2026-08-08 廃止。** 招待URLを公開コードに置かない方針へ変更したため、定数ごと削除した（→ §6「Discordの招待URL」） |

**`YOUTUBE_JOIN_URL`** = `https://www.youtube.com/@AI音楽部-AImusic/join`
（通常のチャンネルページではなく、**メンバーシップ加入画面へ直接遷移**させる）
プラン画面の Premium・Creator・Master 各カードの「YouTubeで加入する」ボタンが
**この1つの定数**を参照する。カードごとに別のURLを書かないこと。

**`OFFICIAL_LINE_URL`** = `https://lin.ee/OxFaiLT`

⚠️ **Discordの招待URLとは性質が違うので、混同しないこと。**
Discordの招待URLは「定員30名の私的コミュニティへの入場鍵」で、公開すると定員そのものが
崩れるため公開コードに置かない（→ §6）。一方この公式LINE URLは、**既にYouTubeメンバー
限定投稿で公開案内している問い合わせ窓口**であり、誰が開いても不都合が生じる性質のもの
ではない。そのため `YOUTUBE_JOIN_URL` と同じ扱いで `role.ts` に定数として1箇所だけ持つ
（2026-08-11 カラスイさんの判断）。画面側にURLを直接書かないのは他の定数と同じ理由
（変更時の直しどころを1箇所にするため）。

用途：利用申請の窓口に加えて、**Premium→Master等のプラン変更申告の連絡窓口**としても使う
（→ 下記「プラン変更（アップグレード／ダウングレード）の運用」）。

### ⚠️ role は「表示の出し分け」専用。権限の根拠ではない（✅ 2026-08-08 実装済み）

**画面側の `role` は、何を見せるかを決めるためだけのものです。**
限定コンテンツの本文は、この値がどうであろうと手に入りません。
本文はAPIが持っていて、APIはサーバー側で検証してからしか返さないためです。

| | 本番（PROD） | 開発（DEV） |
|---|---|---|
| `localStorage` の role を読むか | **読まない。**初期値は常に `free` | 読む（DevRoleSwitcher用） |
| role が変わる経路 | `setRoleFromSession()` のみ（＝APIの検証結果） | 上記＋DevRoleSwitcher |
| localStorage を書き換えたら | **何も変わらない**（本番実測で確認済み） | 見た目だけ変わる。本文は届かない |

保存先（localStorage）：
- `studio-next:role` … Role（**開発時のみ参照。本番では無視**）
- `studio-next:role-source` … 入手経路（同上）
- `studio-next:session` … APIが発行したセッショントークン。**これだけが本番で意味を持つ**

---

## 5-A. 限定コンテンツの配信（✅ 2026-08-08 本番稼働中）

### 原則：隠すのではなく、渡さない

以前は本文をフロントへ配ったうえでCSSでぼかしていました。
これは**アクセス制御ではなく見た目の細工**であり、
`public/content/home.json` を直接開けば誰でも全文を読めていました。

現在は、**権限のない相手には本文そのものを返しません。**
画面のロック表示は「持っていない」結果であって、原因ではありません。

```
公開されるもの（public/content/home.json）
  prompts[].premium: true      … Premium限定かどうか（ロック表示の判断用）
  prompts[].text: ""           … 本文は空
  pickNoteIds: ["aiip"]        … どの記事にメモが「あるか」だけ
  picks[].free                 … 無料で読める範囲

公開されないもの（Cloudflare KV `premium:content`）
  プロンプト本文 / Pick全文 / 制作メモ本文
```

**`public/` に置いたものは公開物である**——これを不変の前提として扱ってください。

### 構成

```
ブラウザ ──① ライセンスキー ──────▶ Worker
         ◀─② セッショントークン（HS256・30日）─
         ──③ Bearer トークン ──────▶ Worker ──▶ KV `premium:content`
         ◀─④ 本文（権限があるときだけ）────
```

| 項目 | 値 |
|---|---|
| 本番URL | `https://studio-next-api.karasui1969.workers.dev` |
| ソース | `worker/api/` |
| KV binding | `ROSTER_KV` → 名前空間 **`STUDIO_API_KV`** (`6e574599dff34a6d9c865aadc0225b91`) |
| フロントの接続先 | `.env.production` の `VITE_API_BASE` |

**⚠️ KVは PoC 用（名前空間 `ROSTER_KV` / `eaacc45e…`）とは別に採番しています。**
PoC はいずれ名前空間ごと削除する予定で、同居させると本番データが巻き添えで消えるためです。

### エンドポイント

| メソッド | パス | 認証 | 返すもの |
|---|---|---|---|
| POST | `/api/session` | 不要 | 資格情報を検証し、セッショントークンを発行 |
| GET | `/api/me` | 任意 | 検証済みの role。無効なら `free` |
| GET | `/api/content/premium` | **premium以上** | プロンプト本文・Pick全文・制作メモ |
| GET | `/api/content/master` | **masterのみ** | Master限定データ |
| GET | `/api/health` | 不要 | 死活・**件数のみ**（中身は返さない） |

未認証は `401`、権限不足は `403`。**両者を区別する**のは案内文を出し分けるためです
（「ログインしてください」と「プランが必要です」は別物）。

### セッショントークン

| 項目 | 現状仕様 |
|---|---|
| 方式 | JWT / **HS256（共通鍵）** |
| **有効期限** | **30日**（2026-08-10変更・元は24時間） |
| 検証者 | **Workerのみ**。フロントは検証しない |
| 保存先 | `localStorage['studio-next:session']` |
| 期限切れの扱い | 401 → ロック表示に戻る。プラン画面に**有効期限を事前表示**（2026-08-10追加）。自動再ログイン導線は未実装（同じキーの手動再入力） |
| 最終ログイン記録 | ライセンスKV記録に `lastLoginAt` を書き戻す（2026-08-10追加）。`scripts/check-license-expiry.mjs <連番>` で実際の失効予定日を確認できる |

#### ⚠️ 有効期限は「30日」で確定。ただし**Workerを再デプロイするまで本番は24時間**

2026-08-11 に本番の稼働状況を実測して判明した食い違いです。

| | 値 | 根拠 |
|---|---|---|
| **仕様として確定した値** | **30日** | 2026-08-10 のカラスイさんの決定（「キーの期限を1ヶ月にします」） |
| ソースコード（`session.ts`） | 30日 | `TTL_SECONDS = 60 * 60 * 24 * 30`（2026-08-10 20:45 に変更） |
| **本番で実際に動いている値** | **24時間** | 本番Workerの最終デプロイが **2026-08-08 00:26**。TTL変更より前 |

つまり30日化は**コード上だけの変更で、まだ本番へ出ていません**。
同じデプロイに含まれていない機能が他にもあります。

- `lastLoginAt` の記録（`membership.ts`）
- `/api/me` が `exp` を返すこと（`index.ts`）
  → これが無いとプラン画面の**有効期限表示が出ません**（`exp: null` になるため）

**本番公開の手順に、フロントのデプロイだけでなくWorkerの再デプロイを必ず含めること。**

```bash
cd worker/api && npx wrangler deploy
```

再デプロイするまでは、**退会対応の説明も「最大24時間」が正しい**値です。
`scripts/check-license-expiry.mjs` は30日前提で計算するため、
再デプロイ前に使うと実際より長い日付を表示します。この点でも先に出す方が安全です。

**HS256を選んだ理由**：当初設計は RS256 でした（フロントでも署名を検証できるため）。
しかし今回の要件は逆で、**フロントに権限判定をさせない**ことが目的です。
検証者が Worker だけなら共通鍵で足り、鍵ペアの管理も配布も不要になります。
フロントはトークンを「預かって送るだけ」の存在に徹します。

**遮断を確認済みの攻撃**（2026-08-08 本番実測）：
トークンの role 書き換え / `alg=none` へのすり替え / 別環境の鍵で作ったトークン /
失効済みキー / 許可外オリジンからのCORS / localStorage の改ざん。

### 権限判定の正 — `resolveMembership()`

**`worker/api/src/membership.ts` の `resolveMembership()` が、role を決める唯一の場所です。**

クライアントが送ってきた role は**一切信用しません**。
判定結果は署名付きトークンに封じてから返します。

```
resolveMembership(credential, env) → { role, source } | null
  ├── mode: 'license'  … ライセンスキー → KVの名簿（ハッシュ照合）  ← 現在稼働中
  ├── mode: 'youtube'  … チャンネルID → 会員名簿                    ← 未実装（差し替え先）
  └── mode: 'dev'      … 開発時のみ。DEV_ROLE_OVERRIDE='true' が条件
```

`null` を返したらセッションを発行しません（＝`free` ですらなく、認証失敗）。

### ✅ 個別ライセンスキー方式が「当面の本番方式」（2026-08-08 決定）

**当初は Google/YouTube 認証までの暫定策と位置づけていましたが、
`members.list` が使えないことが実機で確定したため（上記）、当面の本番方式に格上げしました。**

| | 個別ライセンスキー（採用） | CSV名簿（案A'） | 目視承認（案A''） |
|---|---|---|---|
| 会員判定 | 運営者が発行時に確認 | CSV × OAuth照合 | 運営者が目視 |
| Googleログイン | **不要** | 必要 | 必要 |
| 独自ドメイン | **不要** | 必須 | 必須 |
| OAuth審査 | **不要** | 約10日 | 約10日 |
| 実装状況 | **本番稼働中** | 未実装 | 未実装 |

**採用理由**：Master上限30名という規模では手動運用で回ります。
②③が与えるのは「会員確認の自動化」だけで、そのために独自ドメイン・審査10日・
プライバシーポリシー整備を先に済ませる必要があります。
売上がまだゼロの段階でその順序は取りません。

**この方式の弱点（正直に）**：退会してもキーは自動で無効になりません。
ただしこれは案A'でも同じで（CSVを定期同期しなければ退会者は残る）、
どちらも運用の規律が要る点は変わりません。→ 月1回の棚卸しで対処します。

**なお `resolveMembership()` に Google/YouTube 判定を差し込める構造は維持します。**
Partner Manager が付いた場合、`mode: 'youtube'` の中身を実装するだけで移行できます。
画面・トークン発行・限定コンテンツ配信は変更不要です。

### ✅ 運用設計（2026-08-08 決定・2026-08-08 公式LINEに変更）

```
YouTubeメンバー限定投稿（申請導線のみ。キーは載せない）
  → 公式LINE（https://lin.ee/OxFaiLT）に登録し、トークで利用申請
  → 運営側で YouTube Studio の会員一覧と照合
  → 個別ライセンスキーを発行（1人1キー）
  → LINEのトークで本人にだけ送付
  → Studioでキー入力
  → Premium / Master 権限付与
```

**申請の受け口は当初Googleフォームで設計していたが、公式LINEへ変更した。**
理由：①既存メンバー限定投稿で「MV作りたい部の方は公式LINEへ」と既に案内済みで、
新しい導線を増やすより自然 ②メールアドレスを新たに預かる必要がなくなる。

⚠️ **限定投稿にキーそのものを載せてはいけません。**
全員が同じキーを見ることになり、1人1キーが成立しません。
流出時に該当者だけを失効させられる、という最大の利点が失われます。

⚠️ **公式LINEのURLは1本に統一すること。** 過去の投稿ごとに異なる `lin.ee` リンクが
貼られていた時期があるが、2026-08-08にカラスイさんが実際に開いて確認し、
すべて同一の登録先（`https://lin.ee/OxFaiLT`）であることを確認済み。
今後の投稿・案内文はすべてこのURLに統一する。

#### 申請の受付（公式LINE）

トークで**必ず2つ**を確認してから発行する。この2つが無いと会員一覧と照合できない。

| 項目 | 用途 |
|---|---|
| YouTube表示名 | 会員一覧との照合 |
| 現在のメンバーシッププラン | 発行するRoleの判断 |

**メールアドレスは収集しない**（LINEのトークでキーを送るため不要）。

自己申告のプランは鵜呑みにせず、**必ずYouTube Studioの会員一覧で本人確認してから発行する。**
定型の返信文はカラスイさんの手元にある（申請受付時／キー送付時／690円プランの方向け、の3種）。

#### 連番（プラン別）

```
Premium : P-001, P-002, …
Master  : M-001, M-002, … M-030
```

**Master は30名が上限です。`M-031` の発行はスクリプトが機械的に拒否します**（警告ではなくエラー）。
上限はUIでも常時「少人数制（30名まで）」と表示しています。

#### KVに保存するもの（氏名・メールは入れない）

```
license:<キーのハッシュ> → { role, serial, issuedAt, revoked? }
```

**誰に渡したかはKVが知りません。**運営者のMac上の台帳だけが知っています。
KVが漏れても個人は特定できない、という形にしています。

#### ⚠️ `LICENSE_PEPPER` は1箇所に確定保存し、発行のたびに手入力しない（2026-08-12 教訓）

初回の実ユーザー発行（カラスイさん本人）で、発行時に手入力した`LICENSE_PEPPER`と
本番Workerに`wrangler secret`で設定済みの値が食い違い、正しく発行したはずのキーが
2回連続で認証を通らないという障害が起きた（詳細は変更履歴・軍配DECISIONS.md）。

**pepperは「覚えていて毎回手打ちする」運用では事故る。** パスワード管理ツール等
1箇所に確定保存し、発行のたびにそこからコピーすること。値を変更した場合は、
それ以前に発行した全キーが以後絶対に一致しなくなる点も忘れないこと。

#### 会員管理台帳

**置き場所：`~/軍配/ledgers/LICENSES.md`**（公開リポジトリの**外**。Gitに入る余地がない）

| 連番 | プラン | 発行日 | 申請者 | 状態 | 失効日 | 備考 |
|---|---|---|---|---|---|---|

**キー本体は台帳に書きません。**漏れたら意味がなくなるためで、連番で紐づけます。

#### 退会者の棚卸し（毎月1日・遅くとも3日まで）

```
1. YouTube Studio → 収益化 → メンバーシップ で現在の会員一覧を開く
2. 台帳の「有効」な行と突き合わせる
3. 一覧に居ない人 → node scripts/revoke-license-key.mjs <連番>
4. 台帳に失効日が自動で記入される
```

**失効の効き方（重要）**：失効後、**新しいセッションは発行されません**。
ただし**発行済みセッションは最大30日有効**です（セッションのTTL。2026-08-10に24時間から延長）。
つまり**失効は即時ではなく、最大30日以内に効きます**。トークンを無効化するブラックリスト等は無いため、
退会直後に入れ直されていた場合が最悪ケースです。セキュリティ検証（2026-08-10）で
コード側の攻撃（価格偽装・トークン改ざん等）はすべて防げることを確認済みで、
残るのはこの運用上の遅延のみです。30名規模では許容範囲としていますが、
退会対応時に実際の失効予定日を知りたい場合は
`node scripts/check-license-expiry.mjs <連番>` で個別に確認できます
（最終ログイン日時 or 発行日 + 30日を表示）。

### ✅ プラン変更（アップグレード／ダウングレード）の運用（2026-08-11 決定）

初回発行と同じ「LINEで申告 → 目視確認 → 発行」の型を、プラン変更にもそのまま使う。
新しい判定ロジックは作らない。

```
① 利用者がYouTubeで上位プラン（例：Master）へ変更する
② 公式LINEから「Masterに変更しました」と連絡する（自動判定はしない）
③ 運営者がYouTube Studioの会員一覧を目視で確認する
④ 新しい上位プランのキーを発行する（旧キーのroleは書き換えない）
⑤ 新しいキーを公式LINEで本人に送る
⑥ 本人がプラン画面に新しいキーを入力する
⑦ 旧キーを失効する
```

対象となる経路は Premium→Creator / Premium→Master / Creator→Master の3通り
（およびその逆のダウングレード）。**どれも同じ型で扱い、専用ロジックは作らない。**
連番の頭文字は Premium=`P` / Creator=`C` / Master=`M`
（`scripts/license-ledger.mjs` の `PREFIX`）。

**対象プラン**（→ §4 の価格表と同一。ここでも重複して明記する）

| Premium対象 | Master対象 |
|---|---|
| 「AiでMVを作りたい部」1,290円 | 「AiでMVを作りたい部1ヶ月でマスターしたい方向け」5,600円 |

⚠️ **35,000円「企業様向け」プランはStudioの権限体系と無関係。**
このプラン変更フローの対象にも含めない。価格が高いことを理由にMaster扱いにしない
（→ §4「35,000円『企業様向け』プランの扱い」）。

**なぜ「既存キーのrole書き換え」にしないのか**

KVのレコードは `license:<ハッシュ> → { role, serial, issuedAt, revoked? }` で、
発行された時点のroleに固定されている。これを書き換える経路を作ると、
「誰が・いつ・どのキーのroleを変えたか」が台帳に残らず、
発行スクリプトが前提にしている「1キー＝1回の発行判断」が崩れる。
**新規発行＋旧キー失効**なら、台帳上に

```
P-005 | premium | ... | 失効 | ... | ⚠️ / MasterのM-004へ変更のため失効
M-004 | master  | ... | 有効 | —  | ⚠️ / PremiumのP-005から変更
```

のように**両方向から追える履歴**が残る。実装コストもほぼ変わらない
（`issue-license-key.mjs` と `revoke-license-key.mjs` に任意の `--note` を追加しただけ）。

**コマンド**

```bash
node scripts/issue-license-key.mjs master --name "YouTube表示名" --note "PremiumのP-005から変更" --prod
node scripts/revoke-license-key.mjs P-005 --note "MasterのM-004へ変更のため失効" --prod
```

`--note` は省略可能。省略時は従来どおりの動作（後方互換）。

**Master → Premium のダウングレードも同じ考え方**（専用UIは今回作らない）

```
① MasterからPremiumへYouTubeメンバーシップを変更したと公式LINEで申告
② 運営者が目視確認
③ 新しいPremiumキーを発行（旧Masterキーのroleは書き換えない）
④ 本人へ送付・入力
⑤ 旧Masterキーを失効
```

Masterの枠（30名上限）は「いま有効なMasterが何人か」で判定するため
（`checkMasterLimit()`）、ダウングレードで旧Masterキーを失効させると
**自動的に1枠空く**。新しいMasterを発行する前に慌てて失効させる必要はない。

**キー失効とセッションTTL（30日）の関係**

新キー発行→本人が入力、という順序を守れば、本人は新しいセッションを得ているため
実害はない。失効した旧キーで残るのは「失効前にログインしていた別端末のセッションが
最大30日残る」ことだけで、これは通常の失効と同じ既知の仕様（上記「失効の効き方」）。
**「失効した旧キーで新しいセッションを発行できないこと」が成功条件であり、
「開いている画面から即座に消えること」は成功条件にしない。**

### 開発用の権限切替（DevRoleSwitcher）

**開発環境でのみ使えます。本番ビルドには含まれません。**

`src/app/DevRoleSwitcher.tsx`。押すと見た目を変えるだけでなく、
**APIに本物のセッションを発行させて**限定コンテンツを取りに行きます。
QAが本番と同じ経路を通るので、「見た目は解除されたのに中身が出ない」を
配信側の不具合として検出できます。

安全装置は二重で、片方を外してももう片方で止まります。

1. `import.meta.env.PROD` により本番ビルドで**描画されない**（文字列も残らない）
2. Worker側が `DEV_ROLE_OVERRIDE === 'true'` のときしか `mode: 'dev'` を受け付けない
   （この変数は `.dev.vars` にしかなく、**本番では undefined**）

本番の `/api/health` で `devRoleOverride: false` を確認できます。

### 限定コンテンツの投入と更新

**Worker には埋め込みません。KVに置きます。**
埋め込むと①内容を直すたびに再デプロイが要る②手元にファイルが無い環境（CI）から
デプロイすると内容が消える、という事故が起きるためです。

```bash
node scripts/push-premium-content.mjs           # 本番KVへ投入
node scripts/push-premium-content.mjs --local   # ローカル(wrangler dev)のKVへ
node scripts/push-premium-content.mjs --check   # 投入せず現状だけ確認
```

| 事項 | 内容 |
|---|---|
| 執筆場所 | `content/premium.json`（**Git管理外**。Publicリポジトリに載せないため） |
| 配信元 | KV の `premium:content` |
| **反映の確認** | `/api/health` の **`contentLoaded`** が `true` か。`false` なら未投入 |
| 反映までの時間 | KVは即時ではない。**実測で約30秒** |
| **空ファイル投入の防止** | 中身が0件のときスクリプトが**中止**する。誤って限定コンテンツを消す事故を防ぐ |
| 再デプロイ時 | **消えない**（KVはWorkerと独立。実測で確認済み） |

⚠️ `content/premium.json` は Git管理外です。**失うと復元できません。必ずバックアップしてください。**

⚠️ 2026-08-08以前に公開JSONへ入っていた3件（premiumプロンプト1・Pick全文1・制作メモ1）は
**Git履歴に残っており、すでに公開済みとして扱います**（カラスイさん決裁・2026-08-08）。
履歴の書き換えは行わず、**内容を新しいものに差し替える**方針です。

### 🔴 【確定・2026-08-08】Channel Memberships API は使えない

**実機テストの結果、`members.list` / `membershipsLevels.list` は 403 を返します。**
以下の設計のうち「案A（`members.list` で会員を直接引く）」は**採用できません。**

| エンドポイント | HTTP | reason | message |
|---|---|---|---|
| `members.list` | **403** | `forbidden` | Access forbidden. The request may not be properly authorized. |
| `membershipsLevels.list` | **403** | `forbidden` | 同上 |

**403は「設定不備」ではありません。**次をすべて満たしたうえでの403です。

- OAuth認証は成功し、**AI音楽部のチャンネルとして認証された**
- 認可画面で `youtube.readonly` ＋ `youtube.channel-memberships.creator` の両方を承認
- Console登録だけでなく、実際の認可リクエストにも両スコープが載っていることを傍受確認済み

公式ドキュメントの「Reach out to your Google or YouTube representative to request
access」という記述、および **Partner Manager が付いていない**（2026-08-04確認）事実と整合します。
**申請フォームは存在しません**（[members.list](https://developers.google.com/youtube/v3/docs/members/list)）。

**この結論が覆る条件**：YouTubeからPartner Managerが付いたとき。
こちらから申請する手段は無いため、付いたら見直す、という扱いにします。

### 認証設計（🔴 案Aは不採用が確定 ／ 案A'・A''は未検証）

> **⚠️ 以下は当時の設計資料です。**
> **`members.list` を使う案Aは上記のとおり採用できません。**
> 案A'（CSV名簿）・案A''（目視承認）は、OAuthでチャンネルIDを得る部分は生きていますが、
> いずれも独自ドメイン取得とOAuth審査（約10日）が前提になります。
> 現在稼働しているのはライセンスキー方式です（§5-A）。
>
> なお本節の **RS256／2トークン方式（24時間＋90日）** は当時の設計で、
> 実装済みのセッションは **HS256・30日の単一トークン（2026-08-10延長）**です（理由は §5-A）。
>
> なお本節に出てくる **RS256／2トークン方式（24時間＋90日）** は当時の設計で、
> 実装済みのセッションは **HS256・30日の単一トークン（2026-08-10延長）**です（理由は §5-A）。
> 差し替え時にどちらを採るかは、そのとき改めて決めます。

#### 決定事項（2026-08-04・カラスイさん決裁）

| # | 決定 |
|---|---|
| 1 | 認証方式は**案A（Google OAuth + YouTube Data API 直接連携）**を採用 |
| 2 | **案B（Discord経由）は不採用**。Discord未参加・YouTube未連携・ロール反映遅延により、正規のYouTubeメンバーが利用できなくなる恐れがあるため。Discord連携は「将来の追加機能」として認証とは分離する |
| 3 | V1憲章の「ログイン禁止」原則は、**認証処理に限り例外**として認める |

**例外を認めても、次は維持する（✅ 決定・変更禁止）**

- 音源・歌詞・画像・プロンプト・プロジェクトデータなどの**制作データはサーバーへ送信・保存しない**
- 外部へ送るのは、Google／YouTube認証とメンバーシップ確認に必要な**最小限の情報だけ**
- 運営者のOAuth refresh token・署名用秘密鍵は **Cloudflare Workers の Secrets にのみ**保存する
- 現在の localStorage による Role 判定は、正式認証の完成後に**完全廃止**する
- Google OAuth では **state と PKCE** を必ず使う
- ユーザーのYouTubeチャンネルIDと members.list を照合して本人確認する
- Premium／Master の判定は**クライアントではなく Worker 側**で行う
- セッションは **24時間程度の短期有効期限**とする
- CORSだけに頼らない。**署名検証・レート制限・入力検証**もあわせて実施する
- 認証失敗・期限切れ・退会確認時は **free に戻す**

---

#### 🔴 最重要：members.list は個人クリエイターには開放されていない

**2026-08-04 に公式ドキュメントとGoogleの公式回答を確認した結果**、案Aの中核である
`members.list` は**誰でも使えるAPIではない**ことが判明しました。

> 公式ドキュメントの記載：
> このエンドポイントは、チャンネルメンバーシップを有効にした自分のチャンネルに対して
> 個々のクリエイターが使うためのもので、**アクセスにはGoogleまたはYouTubeの担当者への
> 問い合わせが必要**。

Googleの公式回答（Issue Tracker）はさらに明確で、
**「Partner Manager または Google Representative 経由でしかリクエストできない」**
とされています。Partner Manager は一定規模以上のチャンネルに付く担当者で、
**セルフサービスの申請フォームは存在しません**。

実際、権限のない状態で呼ぶと **403 Forbidden**（`Access forbidden. The request may not
be properly authorized.`）が返ります。個人開発者がこの権限を得られた確認事例は
見つかりませんでした。

**結論：案Aの構成のまま作れるが、`members.list` の1箇所だけは
カラスイさんに Partner Manager が付くまで動かせない可能性が高い。**

対応方針は下の **「案A'：橋渡し構成」** に記載します。
アーキテクチャは案Aのまま、**この1箇所だけ差し替え可能な形にして作る**のが結論です。

- ❓ **確認してほしいこと**：カラスイさんのYouTubeチャンネルに
  Partner Manager／YouTube担当者は付いていますか？
  付いていれば案Aをそのまま完成させられます。

---

#### 全体像（案Aの正式構成）

```
[ブラウザ / GitHub Pages]          [Cloudflare Workers]         [Google / YouTube]
        |                                  |                            |
  ①「Googleでログイン」                    |                            |
  PKCE verifier + state 生成               |                            |
        |------------- ② 認可リクエスト（ブラウザを遷移） ------------->|
        |<------------ ③ 認可コードを持って戻る -----------------------|
        |                                  |                            |
        |--- ④ code + verifier をPOST ---->|                            |
        |                                  |--- ⑤ コード→ユーザーtoken -->|
        |                                  |<-- ユーザーの access token --|
        |                                  |--- ⑥ channels.list(mine) --->|
        |                                  |<-- ユーザーのチャンネルID ----|
        |                                  |  （ここでユーザーtokenは破棄）|
        |                                  |--- ⑦ members.list ---------->|
        |                                  |    （運営者tokenで照合）      |
        |                                  |<-- 会員か／どの段か ----------|
        |<-- ⑧ 署名付きセッション(JWT) ----|                            |
        |                                  |                            |
  ⑨ 公開鍵で署名検証 → useRole() に反映     |                            |
```

**設計の要点**：ユーザーが自分のチャンネルIDを名乗るのではなく、
**Googleが発行したトークンからWorkerが読み取る**。だから詐称できません。
Role判定もWorker側で完結し、クライアントは結果を受け取るだけです。

---

#### 1. Google OAuth 認証フロー

フロントエンド（GitHub Pages）は**シークレットを持てない公開クライアント**なので、
**Authorization Code + PKCE** を使います。client secret は Worker のみが持ちます。

**手順**

| # | 場所 | 処理 |
|---|---|---|
| 1 | ブラウザ | `code_verifier`（ランダム64文字）と `state`（ランダム32文字）を生成し `sessionStorage` に保存 |
| 2 | ブラウザ | `code_challenge = BASE64URL(SHA-256(code_verifier))` を計算 |
| 3 | ブラウザ | `https://accounts.google.com/o/oauth2/v2/auth` へ遷移 |
| 4 | Google | ユーザーが同意 → `redirect_uri?code=...&state=...` へ戻る |
| 5 | ブラウザ | **`state` が一致するか検証**（不一致なら中断＝CSRF対策） |
| 6 | ブラウザ | `{ code, code_verifier }` を Worker の `POST /auth/callback` へ送信 |
| 7 | Worker | client_id + client_secret + code_verifier でトークン交換 |
| 8 | Worker | 手順2（チャンネルID取得）へ |

**認可リクエストのパラメータ**

| パラメータ | 値 | 備考 |
|---|---|---|
| `client_id` | Google Cloud で発行 | 公開してよい |
| `redirect_uri` | `https://karasui1014.github.io/studio-next/` | Google側に完全一致で登録 |
| `response_type` | `code` | |
| `scope` | `https://www.googleapis.com/auth/youtube.readonly` | **これだけ**。メールも名前も取らない（最小限の原則） |
| `code_challenge` | 上記で計算した値 | |
| `code_challenge_method` | `S256` | |
| `state` | ランダム値 | CSRF対策 |

**⚠️ ハマりどころ：HashRouter と redirect_uri**

Studio Next は HashRouter（`#/plans` 形式）ですが、
**Googleは redirect_uri にフラグメント（`#`）を許可しません。**
そのため redirect_uri はベースURLのみを登録し、
戻ってきたら `window.location.search` の `?code=` を検出して処理します。
処理後は `history.replaceState` でクエリを消してから通常の画面へ遷移させます。

**⚠️ ユーザーのトークンは保存しない**

ユーザーの access token は Worker が**チャンネルID取得に1回使うだけで即破棄**します。
refresh token は要求しません（`access_type=offline` を付けない）。
Studio側にユーザーのGoogleトークンは一切残りません。

---

#### 2. ユーザーのYouTubeチャンネルIDを取得する方法

```
GET https://www.googleapis.com/youtube/v3/channels?part=id&mine=true
Authorization: Bearer {ユーザーのaccess token}
```

- `items[0].id` が本人のチャンネルID（`UC...`）
- **必ずWorker側で取得する。**クライアントから送られてきたチャンネルIDは絶対に信用しない
  （信用すると、他人のチャンネルIDを送るだけで権限を奪える）
- クォータ消費：1ユニット

**⚠️ ハマりどころ2つ**

| 状況 | 起きること | 対応 |
|---|---|---|
| **YouTubeチャンネル未作成のGoogleアカウント** | `items` が空配列で返る | エラーではなく「チャンネルがありません」として案内（→ 項目8） |
| **ブランドアカウントで複数チャンネルを持っている** | `mine=true` は**同意画面で選んだチャンネル**を返す。メンバーシップを持つチャンネルと別だと照合に失敗する | 「別のアカウントで試す」導線を用意し、`prompt=select_account` で再認証させる |

---

#### 3. members.list との照合方法

```
GET https://www.googleapis.com/youtube/v3/members
      ?part=snippet
      &filterByMemberChannelId={ユーザーのチャンネルID}
      &mode=all_current
Authorization: Bearer {運営者のaccess token}
```

- `filterByMemberChannelId` は**特定チャンネルの会員状態だけをピンポイントで確認**できる
  （最大100件までカンマ区切り。今回は常に1件）
- 全会員リストを取得してループする必要はない＝**会員が増えても処理量は変わらない**
- クォータ消費：**2ユニット／回**（1日1万ユニットの上限に対して余裕がある）

**レスポンスから読む項目**

| フィールド | 用途 |
|---|---|
| `items[].snippet.memberDetails.channelId` | 念のため要求したIDと一致するか再確認 |
| `items[].snippet.membershipsDetails.highestAccessibleLevel` | **段のID。Role判定はこれを使う** |
| `items[].snippet.membershipsDetails.highestAccessibleLevelDisplayName` | ログ・デバッグ用（判定には使わない） |

**判定ルール**

- `items` が空 → **会員ではない → `free`**
- `items` に1件あり → `highestAccessibleLevel` を Role へ変換（→ 項目4）

**🔴 この呼び出しだけが、上に書いたアクセス権の制約を受けます。**
Workerの中で `checkMembership(channelId) -> { isMember, levelId }` という
**1つの関数に閉じ込めて実装**し、中身を差し替えられるようにします（→ 案A'）。

---

#### 4. 会員レベルIDと premium／master の対応方法

**段の一覧は1回だけ取得して控える**

```
GET https://www.googleapis.com/youtube/v3/membershipsLevels?part=id,snippet
Authorization: Bearer {運営者のaccess token}
```

各段の `id`（不透明な固定文字列）が返るので、それを控えて Worker の
環境変数に設定します。

| YouTubeの段 | 価格 | Role |
|---|---|---|
| Ai大好き部 | ¥690 | **`free`**（Studio権限なし） |
| AI音楽部 Studio Premium | ¥1,290 | `premium` |
| AI音楽部 Studio Creator | ¥3,490 | `creator` |
| AI音楽部Studio Master | ¥5,600 | `master` |
| 「企業様向け」Aiで曲作りしたい部 | ¥35,000 | **`free`**（Studio対象外・→ §4） |

**✅ 対応は「表示名」ではなく「レベルID」で持つ。**
表示名はYouTube側で改名できてしまうため、改名した瞬間に全員の権限が壊れます。
IDは不変なので安全です。

**✅ 未知のレベルIDは `free` に倒す（フェイルクローズ）。**
新しい段を追加したのに Worker のマッピングを更新し忘れた場合、
誤って権限を与えるより、権限を与えない方が安全です。

**✅ `highestAccessibleLevel` を使う。**
段を上げた人が正しく上位Roleになります。

Worker側のマッピングは `src/core/entitlement/role.ts` の `YOUTUBE_TIERS` と
**同じ内容を二重管理**することになります。片方だけ変えないよう、
`role.ts` にコメントで相互参照を書きます。

---

#### 5. 運営者 refresh token の初回取得および更新方法

> **この節は案Aのみに関係します。**
> 案A' では `members.list` を呼ばないため、**運営者のrefresh tokenは不要**です
> （＝下の「7日で失効」問題も発生しません）。

**初回取得（カラスイさんが1回だけ行う作業）**

1. Google Cloud Console でOAuthクライアント（種別：ウェブアプリケーション）を作成
2. YouTube Data API v3 を有効化
3. **チャンネル運営者のGoogleアカウント**で、次のスコープに同意する
   - `https://www.googleapis.com/auth/youtube.channel-memberships.creator`
   - `https://www.googleapis.com/auth/youtube.readonly`
4. その際、認可URLに **`access_type=offline` と `prompt=consent` を必ず付ける**
   （これが無いと refresh token が返りません。最頻出のハマりどころ）
5. 返ってきた refresh token を Secrets に保存
   ```
   wrangler secret put YT_OWNER_REFRESH_TOKEN
   ```

**⚠️ 最大の落とし穴：OAuth同意画面が「テスト中」だと refresh token は7日で失効する**

Google の仕様で、公開ステータスが **「テスト中」のアプリの refresh token は
7日間で無効**になります。毎週カラスイさんが再認証する運用は現実的ではないので、
**同意画面を「本番環境」に切り替える**必要があります。

→ 本番公開には**Googleのアプリ審査が必要**です。要件と所要期間は
  この章の後半「**OAuth審査・本番公開の要件**」にまとめてあります。

**更新（Workerが自動で行う）**

```
POST https://oauth2.googleapis.com/token
  grant_type=refresh_token
  refresh_token={Secretsの値}
  client_id / client_secret
```

- 返ってくる access token の有効期間は約1時間
- **毎回取り直さず、Workers KV に約50分のTTLでキャッシュ**する
  （認証のたびにトークン発行APIを叩くと無駄が多く、レート制限にも当たりやすい）
- refresh token 自体は通常は無期限。ただしカラスイさんがGoogleのパスワードを変更したり
  アプリのアクセス権を取り消すと無効化される

**⚠️ refresh token が無効になったときの挙動（重要）**

`invalid_grant` が返ったら、それは**運営側の障害**であって
ユーザーが退会したわけではありません。
このとき全員を `free` に落とすと、**課金中の会員が一斉に締め出されます。**

→ この場合は Role を変更せず、「一時的に確認できません」として
**既存セッションを有効期限まで維持**し、運営へ通知します（→ 項目7・8）。

---

#### 6. Workerが発行するセッションの形式（✅ 2026-08-04 修正）

**修正の理由**：初版は24時間ごとの再確認を `prompt=none`（Googleへの無言再認証）に
依存させていました。しかし `prompt=none` は**成功が保証されません**。
Googleのセッションが切れている・アカウントを複数使っている・ブラウザが
サードパーティCookieを制限している場合などに失敗し、そのたびにユーザーへ
明示的なログインを求めることになります。**主たる更新手段には使いません。**

**2トークン方式に変更する**

| トークン | 形式 | 有効期限 | 保存場所 | 役割 |
|---|---|---|---|---|
| **アクセストークン** | JWT（RS256署名） | **24時間** | `localStorage: studio-next:session` | Roleを持つ。画面が見るのはこれだけ |
| **リフレッシュトークン** | 不透明なランダム値（256bit） | **90日** | `localStorage: studio-next:refresh` | 24時間ごとの再確認に使う。中身に意味は無い |

**アクセストークン（JWT）に含める情報（最小限）**

| クレーム | 内容 |
|---|---|
| `iss` / `aud` | `studio-next-auth` / `studio-next` |
| `sub` | **チャンネルIDのハッシュ値**（SHA-256 + サーバー側ソルト）。生の値はクライアントへ返さない |
| `role` | `free` / `premium` / `master` |
| `src` | `youtube` |
| `iat` / `exp` | 発行時刻 / 発行から24時間 |
| `stale` | 確認できずに猶予で延長した場合のみ `true`（→ 項目7） |

氏名・メールアドレス・生のチャンネルIDは**含めません**。

**RS256を使う理由**：フロントには公開鍵しか置かないため、
配布物から秘密鍵が漏れる経路が原理的に存在しません。
HS256（共通鍵）だと検証にも同じ鍵が必要で、フロントに置けません。

**再確認の流れ（24時間ごと・ユーザー操作ゼロ）**

```
① クライアントがアクセストークンの exp 切れを検知
② POST /auth/refresh { refreshToken }
③ Worker が KV から refreshToken に紐づくチャンネルIDを引く
④ Worker が【運営者の】refresh token で Googleアクセストークンを更新
   （KVに約50分キャッシュ。毎回は取り直さない）
⑤ members.list（案A）／会員リスト（案A'）で現在の段を確認
⑥ 新しい24時間のアクセストークンを発行して返す
```

**✅ この経路にユーザーのGoogleは一切登場しません。**
Googleへの再ログインが必要になるのは、リフレッシュトークンが90日で切れたとき、
無効化されたとき、ユーザーがログアウトしたときだけです。

**✅ ユーザーのGoogle refresh token は取得も保存もしない**

チャンネルIDが一度確定すれば、以後の会員確認は**運営者のトークンだけ**でできます。
ユーザーのrefresh tokenを預かると、そのユーザーのYouTubeアカウントへ継続的に
アクセスできる強い権限を持つことになり、漏洩時の被害が桁違いになります。
一方チャンネルIDはチャンネルURLに出る**公開情報**なので、預かる情報として明確に軽い。

→ ユーザー側の認可リクエストに **`access_type=offline` は付けません。**
（運営者側の1回きりの認可にだけ付ける。→ 項目5）

**サーバー側に持つデータ（Workers KV）**

| キー | 値 | TTL |
|---|---|---|
| `refresh:{SHA-256(リフレッシュトークン)}` | `{ channelId, issuedAt, lastVerifiedAt }` | 90日 |
| `owner_token` | 運営者のGoogleアクセストークン（キャッシュ） | 50分 |

- **リフレッシュトークンは平文で保存しない**（ハッシュで保存）。
  KVの中身が漏れても、そのままではセッションを乗っ取れません
- チャンネルIDは案Aで `members.list` に渡す必要があるため復元可能な形で持ちます
  （案A'ではハッシュのみで足ります。→ 案A'の節）
- 氏名・メールアドレスは**保存しません**

**⚠️ 正直に書いておく限界（2026-08-08 追記：この結論は §5-A で実装済み）**

`localStorage.setItem('studio-next:role','master')` による「誰でもできる」なりすましは、
**2026-08-08 の実装で塞がれています**（本番実測で確認済み）。

ただし、配信されたJavaScriptを開発者ツールで書き換えられる利用者は、
**画面側の**検証処理を迂回できます。静的サイトである以上これは原理的に避けられません。

→ したがって**本当に守るものはUIで隠さず、
セッションとRoleを検証したWorkerだけが返す**構成にしました（→ §5-A）。
迂回して得られるのは「解除された見た目」だけで、本文は1文字も降ってきません。

---

#### 7. ログアウト・期限切れ・退会時の処理（✅ 2026-08-04 修正）

| 事象 | 検知方法 | 処理 | Role |
|---|---|---|---|
| **ログアウト** | ユーザー操作 | クライアントの2トークンを削除し、`POST /auth/logout` でKVのエントリも削除 | `free` |
| **アクセストークン切れ（24時間）** | `exp` を確認 | **自動で `/auth/refresh`。ユーザー操作なし** | 再確認の結果 |
| **リフレッシュトークン切れ（90日）／無効** | Workerが `401` を返す | トークンを削除し、「メンバーシップの再確認のため、もう一度ログインしてください」 | `free` |
| **退会** | 再確認時に会員として見つからない | KVのエントリを削除し、「メンバーシップが確認できませんでした」＋加入導線 | `free` |
| **確認できなかった（APIエラー等）** | Workerが `503` を返す | **Roleを変えない。**下の「猶予」を適用 | 維持 |

**✅ 「会員でないと確認できた」と「確認自体ができなかった」を必ず区別する。**
これを一緒にすると、YouTube側の一時障害で課金中の会員が全員締め出されます。

**確認できなかったときの猶予（重要）**

アクセストークンが切れているタイミングでYouTube側が落ちていると、
「延長すべきトークンがすでに無い」状態になります。ここで `free` に落とすと、
障害中は課金会員が全員使えません。そこで：

- 直近の確認結果（`lastVerifiedAt` と Role）をもとに、
  **`stale: true` を付けた6時間のアクセストークン**を発行する
- 画面には「メンバーシップを再確認できていません」という控えめな注意だけ出す
- 猶予は**連続3回・合計72時間まで**。それを超えたら `free` に落とす
- 猶予の発行は必ずログに残す（チャンネルIDはハッシュで記録）

**退会の反映タイミング**

YouTubeからのWebhook通知はありません。**最長24時間の遅れ**が発生します
（アクセストークンの有効期限と同じ）。これは仕様として受け入れます。
案A'の場合はさらに**CSV同期の間隔ぶん**遅れます。

#### 8. APIエラー・YouTubeチャンネル未作成ユーザーへの案内

| 状況 | 技術的な条件 | 画面に出す案内 | Role |
|---|---|---|---|
| YouTubeチャンネルが無い | `channels.list` の `items` が空 | 「このGoogleアカウントにはYouTubeチャンネルがありません。メンバーシップに加入したアカウントで、もう一度お試しください」＋チャンネル作成の案内 | `free` |
| アカウント違い／会員でない | `members.list` の `items` が空 | 「メンバーシップを確認できませんでした」＋**「別のアカウントで試す」**＋`YOUTUBE_JOIN_URL` への加入導線 | `free` |
| ユーザーが同意をキャンセル | `error=access_denied` で戻る | 「ログインをキャンセルしました」。エラー扱いにしない | 変更なし |
| クォータ超過 | `403 quotaExceeded` | 「混み合っています。しばらくしてからお試しください」 | **変更なし** |
| 運営者トークンが無効 | `400 invalid_grant` | 「現在メンバーシップの確認ができません。復旧までお待ちください」＋**運営へ通知** | **変更なし** |
| members.list の権限が無い | `403 forbidden` | 同上（→ 案A' へ切り替える判断材料） | **変更なし** |
| レート制限に当たった | 自前の制限で `429` | 「しばらく待ってからお試しください」 | 変更なし |
| `state` 不一致 | CSRFの疑い | 「認証をやり直してください」。処理を中断 | `free` |

**✅ 原則：ユーザー起因（会員でない）は `free` に落とす。
運営・API起因（確認できない）は Role を変えない。**

---

#### 9. 現在の仮実装を削除するタイミング

**削除対象**

| 対象 | 内容 |
|---|---|
| `src/features/plans/PlansPage.tsx` の `MembershipBox()` | ライセンスキー入力欄（`includes('MASTER')` の判定）を「Googleでログイン」に置き換え |
| `src/app/DevRoleSwitcher.tsx` | **ファイルごと削除** |
| localStorage `studio-next:role` / `studio-next:role-source` | 廃止。起動時に**残っている古いキーを削除する処理**を入れる（消さないと過去に自己付与したmasterが残り続ける） |
| `role.ts` の `setRole` | 任意のUIから呼べる形をやめ、**検証済みJWTからのみ**Roleが決まる形にする |

**削除の順序（ここを間違えると自分が締め出される）**

1. 新しい認証フローを**フラグの裏で実装**し、旧実装と併存させる
2. 本番環境で、カラスイさん自身のアカウントで **premium と master の両方**が
   正しく判定されることを確認する
3. 会員でないアカウントで `free` になることを確認する
4. **確認が取れたのと同じPRで**フラグを反転し、上記の削除を行う
5. PROJECT_SPEC.md のこの節を「✅ 実装済み」に更新する

**⚠️ 手順2を飛ばして先に消さないこと。**
認証が動かないまま旧実装を消すと、カラスイさん自身も含め全員が `free` になります。

---

#### 10. YouTube Memberships API の利用可否・アクセス申請要否（調査結果）

| 項目 | 調査結果 |
|---|---|
| 必要なスコープ | `https://www.googleapis.com/auth/youtube.channel-memberships.creator` |
| 誰が呼べるか | **チャンネル運営者本人のみ**（ユーザー本人の資格情報では呼べない） |
| 事前申請 | **必要。** ただし**セルフサービスの申請フォームは存在しない** |
| 申請経路 | **Partner Manager または Google／YouTube の担当者経由のみ** |
| 権限が無い場合 | `403 Forbidden`（`Access forbidden. The request may not be properly authorized.`） |
| 個人開発者の取得事例 | **確認できず** |
| クォータ | 2ユニット／回（`members.list`）。1日1万ユニットの標準枠に対して十分 |
| チャンネル側の前提 | チャンネルメンバーシップが有効になっていること |

**判断**：`members.list` に依存しきった実装は、着手しても完成しない可能性があります。
下の案A'を**同時に用意する**ことを推奨します。

---

#### 案A'：members.list が使えない場合の橋渡し構成（🚧 **成立未確認・実装保留**）

**⛔ 前提：下の「成立条件の検証」を通過するまで、実装に進んではいけません。**
（2026-08-04 カラスイさん指示）

アーキテクチャは案Aと完全に同じで、Worker内の**1関数だけ**を差し替えます。

```
checkMembership(channelId) -> { isMember, levelId }
   ├── 案A  : members.list を呼ぶ            ← 権限が取れたらこちら
   └── 案A' : Workers KV の会員リストを引く   ← 検証を通ったら作れる
```

##### 成立条件（すべて満たさなければ不成立）

| # | 条件 | 状態 |
|---|---|---|
| 1 | YouTube StudioからメンバーCSVを書き出せる | 🚧 未確認 |
| 2 | CSVに**チャンネルID等の不変・一意な識別子**が含まれる | 🚧 **未確認・最重要** |
| 3 | その識別子が、Google OAuth後に `channels.list(mine=true)` で得る値と**同一** | 🚧 未確認 |
| 4 | ブランドアカウント・複数チャンネル環境でも破綻しない | 🚧 未確認 |

**⛔ 表示名（チャンネル名）だけでの照合は禁止**（2026-08-04 決定）。
表示名はいつでも変更でき、重複も可能で、一意でも不変でもありません。
表示名照合は**なりすましを許す**ため、条件2を満たさない場合は
**案A'そのものを不成立として扱い、実装しない。**

##### 成立条件の検証手順（カラスイさんの作業・15分程度）

**手順1：CSVを書き出す**

1. [YouTube Studio](https://studio.youtube.com/) を開く
2. 左メニューの **「収益受け取り」** → **「メンバーシップ」** タブ
   （表示されない場合は左メニュー **「アナリティクス」→「収益」** 経由でも到達可）
3. メンバー一覧の**右上にある「ダウンロード」ボタン**を押す
4. CSVを保存（書き出しに数分かかることがあります）

**手順2：自分のチャンネルIDを取得する**（Google Cloudの設定は不要）

1. [Google OAuth 2.0 Playground](https://developers.google.com/oauthplayground/) を開く
2. 左の一覧から **YouTube Data API v3** →
   `https://www.googleapis.com/auth/youtube.readonly` を選ぶ
3. **Authorize APIs** を押し、**メンバーシップに使っているアカウント**で許可
4. **Exchange authorization code for tokens** を押す
5. 右上の欄に次を入力して **Send the request**
   ```
   https://www.googleapis.com/youtube/v3/channels?part=id&mine=true
   ```
6. 返ってきた `items[0].id`（`UC` で始まる24文字）が自分のチャンネルID

**手順3：照合する**

```bash
node scripts/poc-check-member-csv.mjs ~/Downloads/members.csv UCxxxxxxxxxxxxxxxxxxxxxx
```

このスクリプト（`scripts/poc-check-member-csv.mjs`）は検証専用です。
**氏名やメールアドレスは画面に出さず、すべてマスクして表示します。**
結果を共有するときは画面の出力だけを貼り、CSVそのものは渡さないでください。

**手順4：ブランドアカウントの確認**（条件4）

手順2の認可画面でアカウント選択が出る場合、**複数のチャンネルを持っています。**
その場合は次を確認します。

- 別のチャンネルを選ぶと `channels.list(mine=true)` の戻り値が変わるか
- メンバーシップに使っているチャンネルと、既定で選ばれるチャンネルが一致するか

一致しない環境が普通に起きるなら、ログイン後に
**「このチャンネルで合っていますか？」＋「別のアカウントで試す」** を必ず出します
（`prompt=select_account` で選び直させる）。

##### 判定

- **条件2と3を満たした場合** → 案A'は技術的に成立。下のCSV管理設計へ進む
- **満たさない場合** → **案A'は不成立。実装しない。**
  Partner Manager 経由で案Aの権限を取得できるまで、正式認証は保留とする
  （それまでは現状の仮実装のまま。ただし「なりすまし可能」であることを認識しておく）

##### 見通し（正直な予測）

**条件2は満たされない可能性が高いと見ています。** 根拠は3つです。

1. YouTubeの公式ヘルプはCSVの列を一切公開しておらず、
   説明されている指標は「メンバー歴」「最終更新」など**表示名に紐づく統計**のみ
2. 会員のチャンネルIDを返す `members.list` を Partner Manager 経由に限定している以上、
   同じ情報をCSVで無条件配布するとは考えにくい
3. コミュニティ製のメンバー一覧ツールは、公式に「ほとんど何も無い」として
   **ブラウザのコンソールからのスクレイピング**に頼っている

そのため**先に検証だけ行い、結果を見てから判断する**という進め方が妥当です。

##### 案A'' （条件2が満たされない場合の代替・❓ 検討中）

表示名の自動照合は禁止ですが、**人が目視で確認する運用**なら禁止事項に触れません。

```
① ユーザーがGoogleでログイン → Workerがチャンネルidを確定（詐称不可）
② 管理画面に「承認待ち」として、チャンネルへのリンク付きで並ぶ
③ カラスイさんがYouTube Studioのメンバー一覧と見比べて承認する
④ 承認された【チャンネルID】をキーに権限が付く
```

照合キーはあくまで**検証済みのチャンネルID**で、表示名は人が見るための補助にすぎません。
会員数が少ないうちは現実的で、案Aの権限が取れたら自動化へ移行できます。
- ❓ この運用を採るかは未決定

---

#### 案A' のCSV取り込み機能の設計（🚧 案A'成立時のみ実装）

##### 基本方針：**CSVをサーバーへ送らない**

CSVには氏名などの個人情報が含まれます。そこで、
**取り込みは管理画面のブラウザ内で完結**させ、
サーバーへ送るのは**照合に必要な最小限のデータだけ**にします。

```
[管理画面（ブラウザ）]                      [Cloudflare Workers]
  CSVを選ぶ（アップロードしない）
      ↓
  ブラウザ内で解析
      ↓
  チャンネルID → SHA-256(ID + サーバー側ソルト)
  氏名・メール・日付などは【捨てる】
      ↓
  { ハッシュ, レベルID } の配列だけを送信 -----> 検証して保存
```

これにより、**氏名もメールアドレスもCSVそのものもサーバーに存在しません。**

##### 各項目の設計

| 項目 | 設計 |
|---|---|
| **管理者認証** | 一般ユーザーと同じGoogleログインを使い、`channels.list(mine=true)` が**運営者チャンネルIDと一致**した場合だけ管理APIを許可する。運営者チャンネルIDは Secrets に保管。パスワードを別途作らないので漏洩経路が増えない。加えて管理APIは**別の管理者用シークレット**（`X-Admin-Key`）も要求する二重化 |
| **CSV形式の検証** | ①UTF-8（BOM可）②RFC4180準拠のCSV ③ヘッダー行あり ④1行以上のデータ ⑤上限1万行。いずれかを満たさなければ**取り込み前に中断** |
| **必須列の検証** | 「チャンネルID列」と「レベル列」が特定できることを必須とする。列名は変わりうるので、**値の形（`UC`＋22文字）で自動検出**し、候補が0個なら中断。レベル列は既知のレベル名/IDと突き合わせて検出 |
| **取り込み前のプレビュー** | 解析結果を画面に出し、**確認ボタンを押すまで送信しない**。表示するのは統計とハッシュの断片のみで、**氏名は一切表示しない** |
| **総人数とレベル別人数** | プレビューに `総数 / premium / master / 対象外(AI大好き部など) / 判定不能` を必ず表示。判定不能が1件でもあれば警告を出す |
| **前回との差分** | `追加 N人 / 削除 N人 / レベル変更 N人 / 変化なし N人` を表示。**削除が全体の30%を超える場合は赤字で強調**し、確認チェックボックスを必須にする（CSVの取り違えを防ぐ） |
| **取り込み日時** | `importedAt`（ISO 8601）と `importedBy`（運営者チャンネルIDのハッシュ）と `rowCount` をKVに記録し、管理画面に常時表示 |
| **古いCSVへの警告** | 画面上部に経過日数を表示。**7日超で黄色の警告**、**30日超で赤の警告**。30日超の場合は Studio 側のプラン画面にも「メンバーシップ情報の更新が遅れています」と控えめに表示する（ユーザーが問い合わせる前に気づけるように） |
| **ロールバック** | 取り込みのたびに**版として保存**（`roster:v{n}`）。直近**3版**を保持し、管理画面から1クリックで前の版へ戻せる。現在の版は `roster:current` がポインタとして指す。ロールバックも `importedAt` を更新して履歴に残す |
| **個人情報をログへ出さない** | ログに出してよいのは「件数」「レベル別人数」「差分数」「ハッシュの先頭8文字」だけ。**氏名・メール・生のチャンネルIDは出力禁止**。エラーメッセージにも値を含めず、行番号のみを示す |
| **CSVの保存場所と暗号化** | **CSVはサーバーに保存しない。**ブラウザのメモリ上でのみ扱い、送信もしない。KVに入るのは `SHA-256(チャンネルID + Secretsのソルト)` とレベルIDのみで、これは**元の値へ戻せない**。カラスイさんの手元のCSVは、確認後すみやかに削除する運用とする |
| **データ削除方法** | ①個別：管理画面から対象ハッシュを削除 ②全体：`roster:*` を全削除するとその時点で全員が `free` に戻る ③ユーザー本人からの削除依頼：セッションKV（`refresh:*`）の該当エントリを削除。詳細はプライバシーポリシーに明記する（→ OAuth審査の節） |

##### 同期の運用

- 頻度は**週1回以上**を推奨（30日を超えると赤警告）
- ❓ 将来この作業をどこまで自動化するかは未決定
- 案Aの権限が取れたら、`checkMembership()` の中身を差し替えるだけで移行できる

---

#### Premium／Masterコンテンツの配信（✅ 2026-08-04 決定・認証と同時に実施）

##### 現状の問題

`public/content/home.json` は静的公開ファイルなので、
**URLを直接開けば中身が全部読めます。** 実際に次が入っています。

| 対象 | 状態 |
|---|---|
| Premium限定プロンプト（`premium: true`） | 公開JSONに平文（現在1件） |
| カラスイ Picks の制作メモ（`pickNotes`） | 公開JSONに平文（現在1件） |

権限認証を完璧にしても、**なりすます必要すらなく読めてしまいます。**
画面のぼかしロックは「見えなくしている」だけで、「渡していない」わけではありません。

##### 決定：Worker配信へ移す。認証実装と**同時に**行う

**公開側（`public/content/home.json`）に置いてよいもの＝メタデータだけ**

| 置く | 置かない |
|---|---|
| ID、タイトル、説明文、サムネイル、必要ロール（`requiredRole`）、公開日 | **限定本文、`pickNotes` の本文、Premium限定プロンプトの本文** |

これにより、無料ユーザーにも「そこに何があるか」は見えるまま
（訴求として必要）、中身だけが渡らない状態になります。

**限定本文の配信**

```
GET /content/premium/{id}
Authorization: Bearer {アクセストークン(JWT)}
   ↓ Worker が署名・exp・aud を検証
   ↓ JWT の role が requiredRole を満たすか確認
   ↓ 満たす場合のみ本文を返す
```

- 判定は**必ずWorker側**。クライアントから送られたRoleは信用しない
- 満たさない場合は `403` を返し、**本文は一切返さない**
- 限定本文の保管場所は Workers KV（または管理画面から更新する非公開ストア）

##### ⚠️ Git履歴に残る問題（設計上の明記事項）

**`home.json` から限定情報を消しても、過去のGit履歴には残り続けます。**
リポジトリは Public なので、`git log -p -- public/content/home.json` を実行すれば
誰でも削除前の内容を読めます。ファイルを消すことは、公開を取り消すことにはなりません。

**今回該当する2件（Premium限定プロンプト1件・制作メモ1件）の扱い**

この2件は**すでに公開サイトから誰でも取得できる状態で配信されていました。**
つまり履歴を消しても「すでに公開済み」である事実は変わりません。
一方、公開済みリポジトリの履歴書き換え（`git filter-repo` ＋ force push）は
全コミットのハッシュが変わり、クローンやフォークには残り、
GitHubのキャッシュにも残る可能性があります。

→ **結論：この2件は履歴書き換えを行わず、「すでに公開済み」として扱う。**
   秘匿性を回復させたいなら、**内容そのものを差し替える**（新しいプロンプト・
   新しい制作メモにする）ほうが確実で副作用がありません。

**今後のルール（✅ 決定・変更禁止）**

1. **限定コンテンツを公開リポジトリにコミットしない。**
   最初から Worker 側の非公開ストアにだけ置く
2. 誤ってコミットした場合は「漏洩した」ものとして扱い、
   **履歴書き換えより先に、内容の差し替えを検討する**
3. 秘匿性が高い情報（キー・トークンの類）を誤ってコミットした場合は、
   **①ただちに無効化・再発行 → ②履歴書き換え** の順で対応する
   （履歴書き換えだけでは、すでに取得された値は無効化できない）

---

#### OAuth審査・本番公開の要件（✅ 2026-08-04 調査結果）

##### 1. 利用するOAuthスコープ

| 誰が | スコープ | 用途 | 必要な構成 |
|---|---|---|---|
| **一般ユーザー** | `https://www.googleapis.com/auth/youtube.readonly` | `channels.list(mine=true)` で本人のチャンネルIDを取得する | 案A・案A' **両方で必要** |
| **運営者（1回のみ）** | `https://www.googleapis.com/auth/youtube.channel-memberships.creator` | `members.list` の呼び出し | **案Aのみ**。案A'では不要 |

- ユーザーからメールアドレスや氏名は取得しません（`openid` / `email` / `profile` を要求しない）
- ✅ **案A'を採る場合、運営者側の強いスコープが丸ごと不要になります。**
  審査対象が `youtube.readonly` 1つだけになる点は案A'の隠れた利点です

##### 2. 各スコープの分類

Googleはスコープを**非機微 / 機微（sensitive）/ 制限付き（restricted）**に分類しています。

| 分類 | 必要な手続き | 費用 |
|---|---|---|
| 非機微 | なし | — |
| **機微（sensitive）** | Googleの**アプリ検証**（審査） | 無料 |
| **制限付き（restricted）** | アプリ検証 **＋ 年次のセキュリティ評価（CASA）** | **有料・第三者監査** |

**YouTubeのスコープは「機微」に該当すると見られます。**
Googleが機微スコープの例として挙げているのは
「Googleカレンダーの予定を読む」「連絡先を保存する」「**YouTubeの動画を削除する**」で、
YouTube系がこの層に位置づけられています。
一方「制限付き」は Gmail や Drive の広範なデータアクセスを指し、
こちらに該当すると**有料のCASA監査が毎年必要**になります。

- ⚠️ ただしGoogleの公開FAQから完全なスコープ一覧は取得できませんでした。
- ✅ **確定させる方法（1分で済みます）**：
  Google Cloud Console → 「OAuth同意画面」→「スコープを追加または削除」を開くと、
  スコープ一覧の表に **「機微」「制限付き」のラベルが表示されます。**
  ここで `youtube.readonly` のラベルを確認するのが最も確実です。
  **制限付きだった場合は費用が発生するため、着手前に必ず確認してください。**

##### 3. OAuthアプリ検証の要否

**必要です。** 機微スコープを要求し、かつ次の免除条件に当てはまらないためです。

Googleが認めている免除条件は次の5つです。

| 免除条件 | 今回当てはまるか |
|---|---|
| 個人利用（自分だけ、または**個人的に知っている少人数**のみが使う） | ❌ 不特定の有料会員が使うため該当しない |
| 開発・テスト・ステージング（公開ステータスが「テスト中」） | ❌ 本番公開するため該当しない |
| サービスアカウント自身のデータのみ | ❌ 該当しない |
| 組織内利用のみ（Google Workspace / Cloud Identity の Internal） | ❌ 個人のGmailアカウント運用のため Internal を選べない |
| ドメイン全体へのインストール | ❌ 該当しない |

##### 4. 本番公開までの条件

| 要件 | 内容 |
|---|---|
| **ホームページ** | ログイン不要で閲覧でき、アプリの機能を説明していること。**自分が所有し検証済みのドメイン**上にあること |
| **ドメインの所有権確認** | **Google Search Console** で所有権を確認すること |
| **プライバシーポリシー** | ホームページと**同じドメイン**に置き、同意画面からリンクすること |
| **デモ動画** | YouTubeに**限定公開**でアップロード。**OAuth同意画面を含む一連の流れ**を最初から最後まで、**英語で**示すこと |
| **アプリ名・ブランディング** | 同意画面の表示内容がアプリの実体と一致していること |
| **所要期間** | 機微スコープの審査は**最短10日程度**（実際はやりとりでさらに延びることがあります） |

**🔴 ここで独自ドメインが必要になります（重要）**

現在の公開先は `https://karasui1014.github.io/studio-next/` です。
`github.io` は **GitHubが所有するドメイン**であり、
カラスイさんが Search Console で「所有者」として検証することはできません。
Googleの審査は「**自分が所有するドメイン**」を求めるため、
**github.io のままでは通らない可能性が高い**と見ています。

→ 未決定事項の「独自ドメイン」は、**あれば良いもの**ではなく
   **OAuth審査の前提条件**として扱う必要があります。
   ドメイン取得 → GitHub Pagesにカスタムドメインを設定 → Search Consoleで検証、
   という順で進めます。

##### 5. テスト状態での制限

公開ステータスが「テスト中」のままだと、次の制限がかかります。

| 制限 | 内容 |
|---|---|
| **refresh token が7日で失効** | 運営者のrefresh token（案A）が毎週切れ、そのたびに再認証が必要になる。**運用上ほぼ不可能** |
| **テストユーザーは100人まで** | 事前に登録したアカウントしか利用できない。引き上げには本番公開が必須 |
| ユーザーへの警告 | 「確認されていないアプリ」の警告画面が表示される |

→ 100人以下の限定運用なら「テスト中」でも動かせますが、
   **7日ごとの再認証が必須**になるため、案Aでは実質的に選べません。
   **案A'なら運営者のrefresh tokenが不要なので、この制限を受けません。**
   これは案A'のもう一つの利点です（テストユーザー100人の上限は残ります）。

##### 6. プライバシーポリシーとデータ削除ページの要件

**プライバシーポリシーに必ず書くこと**

Googleは「アプリがGoogleユーザーデータに**どのようにアクセスし、利用し、保存し、
共有するか**」の開示を求めています。今回の構成では次を明記します。

| 項目 | 記載内容 |
|---|---|
| 取得するもの | YouTubeチャンネルID（`youtube.readonly` スコープ経由）**のみ**。氏名・メールアドレス・視聴履歴は取得しない |
| 利用目的 | チャンネルメンバーシップの加入状況を確認し、Studioの利用権限を判定するため**のみ** |
| 保存するもの | チャンネルID（またはそのハッシュ値）、セッション識別子、最終確認日時 |
| 保存しないもの | **制作データ（歌詞・曲・プロンプト・プロジェクト）は一切サーバーへ送信・保存しない**（端末内のみ） |
| 保存期間 | セッションは最長90日。ログアウトまたは削除依頼で即時削除 |
| 第三者提供 | 行わない。Google／YouTube以外への送信はない |
| 削除方法 | 下記 |

**データ削除ページの要件**

削除方法を**ユーザーが自分で実行できる形**で示す必要があります。

1. **アプリ内**：プラン画面に「ログアウトして権限情報を削除」ボタンを置く
   （押すとクライアントの2トークンとKVのセッションエントリが消える）
2. **Google側**：ユーザーはいつでも
   [Googleアカウントのアクセス権管理](https://myaccount.google.com/permissions)
   からStudioの認可を取り消せる旨を案内する
3. **問い合わせ窓口**：メールなどの連絡先を1つ明記し、依頼を受けたら
   該当セッションと（案A'なら）名簿上のハッシュを削除する

- ❓ プライバシーポリシーと削除案内をどのURLに置くかは未決定
  （独自ドメイン取得後、`/privacy` などに設置）

#### セキュリティ設計

| 項目 | 対策 |
|---|---|
| 運営者 refresh token・署名用秘密鍵 | **Cloudflare Workers の Secrets のみ**。リポジトリにもクライアントにも絶対に置かない |
| client secret | Worker のみが保持。フロントには置かない（PKCEを使うのはこのため） |
| ユーザーのGoogleトークン | チャンネルID取得に1回使って**即破棄**。保存しない |
| 署名方式 | **RS256**。フロントには公開鍵のみ |
| アクセストークン有効期限 | **24時間**。切れたらリフレッシュトークンで自動更新（Googleは登場しない） |
| リフレッシュトークン | **90日**。KVには**ハッシュで保存**し、平文で持たない |
| `prompt=none` への依存 | **禁止。**成功が保証されないため、権限更新の主たる手段にしない |
| CSRF | `state` パラメータを必ず検証 |
| 認可コード横取り | **PKCE（S256）** |
| CORS | Worker は Studio のオリジンからのリクエストのみ許可 |
| レート制限 | IP単位で認証エンドポイントに制限（例：10回／分）。KVでカウント。管理APIはさらに厳しく |
| 入力検証 | `code` の形式、`code_verifier` の長さ、チャンネルIDの形式（`UC` + 22文字）を検証してからAPIへ渡す |
| 権限判定の場所 | **必ずWorker側。**クライアントから送られたRoleやチャンネルIDは信用しない |
| 限定コンテンツ | **UIで隠さない。**Workerが署名とRoleを検証したうえで本文を返す |
| 未知のレベルID | **`free` に倒す**（フェイルクローズ） |
| 確認できない場合 | **`free` に倒さない。**猶予付きで維持し、非会員の確定と区別する |
| ログ | チャンネルIDは**ハッシュ化して**記録。氏名・メール・生のIDは出力禁止 |
| 管理API | 運営者チャンネルIDの一致 ＋ 管理者用シークレットの二重化 |
| **CORSだけに頼らない** | CORSはブラウザ側の制限にすぎず、`curl` からは無視できる。**署名検証・レート制限・入力検証を必ず併用する** |

#### 必要な外部サービス

| サービス | 用途 | 費用 | 案A' で必要か |
|---|---|---|---|
| Cloudflare Workers | 認証用サーバーレス関数 | 無料枠で足りる見込み | 必要 |
| Workers KV | セッション、レート制限、限定コンテンツ、（案A'なら）会員名簿 | 無料枠で足りる見込み | 必要 |
| Google Cloud Console | OAuthクライアント発行、YouTube Data API v3 有効化 | 無料 | 必要 |
| **独自ドメイン** | **OAuth審査の前提**（`github.io` は所有権を検証できない） | 年1,000〜2,000円程度 | 必要 |
| Google Search Console | ドメイン所有権の確認 | 無料 | 必要 |
| カラスイさんのYouTube運営者アカウント | `members.list` の呼び出し | — | **不要**（案A'は運営者スコープを使わない） |

#### ユーザー側の手続き

**「Googleでログイン」を1回押すだけ。**
Studioで別途アカウントを作る必要はなく、パスワードの入力もありません。

2回目以降は、**リフレッシュトークンによる自動更新**で権限が復元されます。
この更新にGoogleは登場しないため、画面遷移もアカウント選択も発生しません。
再度ログインを求めるのは、**90日経過時**か、ログアウトした場合だけです。

---

### 検証用の権限切替（→ §5-A「開発用の権限切替」へ統合済み）

DevRoleSwitcher の現行仕様は **§5-A** に記載しています。

**⚠️ 2026-08-08 以前の記述からの変更点（もう当てはまりません）：**

| 以前 | 現在 |
|---|---|
| `MembershipBox()` はキー文字列に `PREMIUM`/`MASTER` が含まれるかを見るだけ | **Workerがサーバー側でKVの名簿と照合**する |
| `localStorage.setItem('studio-next:role','master')` で Master 権限が付く | **本番では何も起きない**（localStorage を権限の根拠にしない） |
| 署名も暗号化もない | **HS256署名付きセッショントークン**。改ざんは401で弾く |
| 限定コンテンツはフロントに配ってCSSでぼかす | **権限が無ければ本文を返さない** |

`PlansPage.tsx` のライセンスキー入力欄は、`/api/session` を呼ぶ形へ差し替える対象として残っています
（§12 未決定事項）。

---

## 6. Discord設計

| 項目 | 値 |
|---|---|
| コミュニティ名 | **AI音楽部 Lab** |
| 対象 | **Master限定** |
| 招待URL | **公開コードに置かない**（控えは `~/軍配/house-rules.md`。Git管理外） |
| Studio側の表示 | 案内文のみ。ボタン・URLは表示しない（2026-08-08変更） |

✅ **決定（2026-08-08 改訂）**：**招待URLはStudio（公開リポジトリ）に一切置きません。**

プラン画面のMasterカードには「招待は公式LINEからお送りします」という案内文だけを表示し、
実際のURLはライセンスキー送付と同じタイミングで**公式LINEから手動送付**します。
公開コードにURLを書くと配信JSに埋め込まれ、一度コミットするとGit履歴からも読めて
取り消せないため。少人数制（30名）の前提を守るため、流通経路をLINEの1本に絞ります。

役割は**交流**です。Studioの中に交流機能は作りません。

### ⚠️ 旧招待URLはすでに公開済み（2026-08-11 記録）

方針変更前の招待URL（`discord.gg/zdG…` で始まるもの。完全な値は
`Initial commit` の `src/core/entitlement/role.ts` にあります）は、
**リポジトリ作成時から平文で入っており、本番配信JSにも埋め込まれていました。**
2026-08-11に公開コードから完全に削除しましたが、次の2つは取り消せません。

1. **Git履歴**：このリポジトリは Public のため、過去のコミットから今でも読めます
2. **すでに取得された分**：削除前にURLを見た人は、そのまま使えます

そのため、コードから消すだけでは不十分です。**Discord側で当該招待リンクを失効させ、
新しい招待URLを作り直す**必要があります（カラスイさんの作業）。
新しいURLは公開コード・Git・仕様書のいずれにも書かず、公式LINEからの手動送付のみとします。

この経緯は「公開リポジトリでは、消しても消えない」という実例として残します。

---

## 7. Substack連携（カラスイ Picks）

### 取得方法

✅ **決定**：**RSS**で取得します。APIは不要でした。

| 項目 | 値 |
|---|---|
| フィードURL | `https://karasui.substack.com/feed` |
| 収集スクリプト | `scripts/collect-picks.mjs` |
| 出力先 | `public/content/picks.json` |
| 実行 | `npm run picks` |
| 自動更新 | GitHub Actions が1日4回 |

**注意**：プロフィールURL `substack.com/@karasui` にRSSはありません。
刊行物のサブドメイン（`karasui.substack.com`）が正しい取得先です。

別の刊行物を使う場合は環境変数 `SUBSTACK_FEED` で切り替えられます。

### 取得する項目

| 項目 | 取得元 |
|---|---|
| タイトル | `<title>` |
| リンク | `<link>` |
| 日付 | `<pubDate>` |
| アイキャッチ | `<enclosure>`（画像のみ。**ポッドキャスト回の音声URLは除外**） |
| 冒頭 | `<content:encoded>` の先頭**300文字** |

### 実装上の注意（ハマりどころ）

1. **Substackが本文に差し込む購読案内を除去すること。**
   除去しないと、記事の冒頭のはずが「ぜひ無料で購読してください」で埋まります。
   `removeSubscribeCta()` で処理しています。

2. **`<enclosure>` は画像とは限りません。**
   ポッドキャスト回では音声URLが入るため、画像判定してから使います。

3. **記事の紐付けキーは記事URLの `/p/` 以降**（例：`aiip`）。

### 表示ルール

✅ **決定**

| 場所 | 表示 |
|---|---|
| ホーム | **最新1件だけ**（`TodayPick.tsx`） |
| Picksページ | **全件**（`PicksPage.tsx`） |

### Studioに本文を載せない理由（✅ 決定・重要）

**冒頭300文字までで、全文はSubstackで読んでもらいます。**

Studioに全文を写すと、Studioだけが育ってSubstackの読者が増えません。
読者数はSubstackの資産なので、そこは削るべきではありません。
「一つ記事を書くたびに3つが同時に育つ」という狙いを守るための判断です。

### Premiumの価値の置き場所

**「本文の続き」ではなく「制作メモ」に置きます。**

記事ごとに、Studio限定の制作メモを **`content/premium.json`（Git管理外）** の
`pickNotes` に手で書きます。キーは記事URLの `/p/` 以降です。

```json
"pickNotes": {
  "aiip": {
    "note": "この記事のIPの考え方は、Studioの曲管理と相性が良いです。…",
    "prompts": ["p-lofi-drive"]
  }
}
```

書いたら **KVへ投入**してはじめて配信されます（§5-A）。

```bash
node scripts/push-premium-content.mjs
```

⚠️ **2026-08-08に置き場所が変わりました。** 以前は `public/content/home.json` の
`pickNotes` に書いていましたが、それでは誰でもURLで全文を読めてしまうためです。
公開側（`public/content/home.json`）に残るのは **`pickNoteIds`** ——
「どの記事にメモがあるか」の一覧だけで、本文は入りません。

これなら無料の人もPremiumの人もSubstackを読みに行きます。
そのうえでPremiumだけが「自分の制作にどう使うか」を受け取れます。

`npm run picks` を実行すると、**制作メモがまだ無い記事を教えてくれます。**

**🚧 既知の課題**：制作メモは記事IDで紐付きますが、記事はRSSで自動更新される一方
メモは手書きのため、**最新記事にメモが無い状態が常態化します**。
現在ホームに出る最新記事にはメモが無く、**Premiumに入ってもホームでは価値が見えません**。
弾切れはエラーにならず無音で進むので、定期的な補充が要ります（§12 未決定事項）。

---

## 8. AI音楽ニュース仕様

### 基本方針

✅ **決定**：**制作の邪魔をしないこと。**

| 場所 | 表示 |
|---|---|
| ホーム | **3件だけ**（控えめな見た目） |
| ニュースページ | 全件＋絞り込み＋検索＋注目ランキング |

### 収集

| 項目 | 値 |
|---|---|
| 情報源 | **28フィード**（`scripts/feeds.json`） |
| 収集スクリプト | `scripts/collect-news.mjs`（外部ライブラリ0） |
| 出力先 | `public/content/news.json` |
| 実行 | `npm run news` |
| 自動更新 | 日本時間 **6:00 / 12:00 / 18:00 / 22:00** |

### 記事の絞り込み（媒体の性質ごとに基準を変える）

✅ **決定**：全部同じ基準にすると精度が落ちます。

| `filter` 値 | 条件 | 向いている媒体 |
|---|---|---|
| `all` | 全部通す | 検索条件自体が絞り込みになっているフィード |
| `ai` | 見出しにAI関連の語 | 音楽専門メディア |
| `music` | 見出しに音楽関連の語 | AI専門メディア |
| `keyword` | 見出しにAIと音楽の語が**両方** | 総合ニュースサイト |

どの場合も「Suno」「音楽生成」などの決定的な語が本文にあれば無条件で採用します。

### 実装上の注意（ハマりどころ）

1. **英数字の語には単語境界 `\b` が必須。**
   これが無いと `udio` が st**udio** / a**udio** に誤爆します（実際に起きました）。

2. **日本語ニュースの網羅には Google News RSS が要です。**
   日本の一般メディアの通常RSSでは、AI音楽記事はほぼ拾えません。

3. **同一記事の重複は名寄せすること。**
   「（1/2枚目）」「(ITmedia NEWS)」のような接尾辞を落として比較します。

### 海外ニュースの日本語化

✅ **決定**：見出しと要約を自動翻訳します。表示は**日本語が主、原文を小さく併記**。

| | |
|---|---|
| 実装 | `scripts/translate.mjs` |
| 主 | MyMemory（公式API・無料枠1日5,000語） |
| 予備 | Google翻訳の非公式エンドポイント |
| APIキー | **不要** |

- 片方が止まっても、もう片方に自動で切り替わります
- **一度訳した記事は記録され、二度と翻訳し直しません**（1回の実行で最大70件）
- 環境変数 `MYMEMORY_EMAIL` を設定すると上限が1日50,000語に増えます（任意）

⚠️ **注意**：これは**機械翻訳**です。配信元の要約を翻訳したもので、
AIが新たに書き起こした要約ではありません。フッターにもその旨を明記しています。

### 注目ランキング

✅ **決定**：ニュースページに**上位5件**を表示します。

「AI音楽をこれから始める人が知りたいか」を基準にスコアリングします。

| 加点 | 点 |
|---|---|
| 使い方・始め方・入門 | +14 |
| 無料で試せる | +12 |
| 日本語対応 | +10 |
| 新機能・リリース | +11 |
| Suno / Lyria 等のサービス名 | +9 |
| 著作権・商用利用 | +7 |
| **複数媒体が報道** | **最大 +24** |

| 減点 | 点 |
|---|---|
| 買収・資金調達・決算 | −8 |
| 機材レビュー | −7 |
| 1週間より古い | −6 |

**1話題につき1枠だけ**載せます（同じ話題で5枠が埋まるのを防ぐため）。
同話題の判定は見出しの文字2つ組の重なり（閾値 **0.13**）と固有名詞の共通部分で行います。
実測値：同話題の組 = 0.19〜0.28 ／ 無関係な組 = 0.04 以下。

---

## 9. データ保存

### 原則

✅ **決定**：**すべて端末内（localStorage）にのみ保存し、外部へは送りません。**

アカウント登録もログインもありません。

### 保存しているもの

| 内容 | キー | 実装 |
|---|---|---|
| 曲データ | `studio-next:songs:v1` | `src/core/storage/songs.ts` |
| 最近ひらいたもの | `studio.recent` | `src/core/storage/activity.ts` |
| お気に入り | `studio.favorites` | `src/core/storage/activity.ts` |
| 連続利用日数 | `studio.visits` | `src/core/storage/streak.ts` |
| 権限 | `studio-next:role` | `src/core/entitlement/role.ts` |
| 入手経路 | `studio-next:role-source` | `src/core/entitlement/role.ts` |
| 表示テーマ | `studio.theme` | `src/core/ui/useTheme.ts` |
| Substack案内を閉じた日 | `studio.substackDismissed` | `src/features/home/sections.tsx` |
| プロンプトのお気に入り | `studio-next:prompt-dex:favorites:v1` | `src/features/tools/prompt-dex/lib/` |

### 「続きから」で記録するもの

記録するのは**「何を開いたか」だけ**です。作品の中身は保存しません。

```ts
interface Activity {
  id: string
  kind: 'song' | 'prompt' | 'tool' | 'external'
  label: string
  to: string | null      // Studio内の遷移先
  url: string | null     // 外部ツールのURL
  at: number             // 最後に触った時刻
}
```

最大20件まで保持します。

### 曲データ

```ts
interface Song {
  id, title, status, genre, mood,
  lyrics, sunoPrompt, mvPrompt, memo,
  createdAt, updatedAt
}

type SongStatus = 'idea' | 'lyrics' | 'suno' | 'mv' | 'published'
```

⚠️ **注意**：V1の `SongStatus` は `'published'` です（`'done'` ではありません）。
V1からロジックを移植する際にここを間違えると型エラーになります。

曲詳細では**入力が止まってから600ms後に自動保存**します
（保存ボタンを押し忘れて消える事故をなくすため）。

### データの持ち出し

✅ **決定**：`/data` から JSON で書き出し・読み込みできます。**この機能は無料のまま維持します。**

読み込みは既存データを消さず、同じIDだけ上書きします。

---

## 10. アーキテクチャ

### Version 2 最大の設計変更：アプリとコンテンツの分離

```
src/                      アプリ（滅多に変わらない）
├── app/                  ルーティング・シェル
│   ├── AppShell.tsx      全体レイアウト
│   ├── Sidebar.tsx       PC用サイドバー
│   ├── TabBar.tsx        スマホ用下部タブ
│   ├── Topbar.tsx        上部バー
│   ├── DevRoleSwitcher   検証用の権限切替（本番では非表示）
│   └── navigation.ts     ナビゲーション定義
├── core/                 全機能が使う土台
│   ├── content/          コンテンツ取得・検証（load.ts / types.ts）
│   ├── entitlement/      権限判定（role.ts）
│   ├── storage/          songs / activity / streak
│   └── ui/               cn.ts / useTheme.ts
└── features/             機能ごとに縦割り
    ├── home/ news/ picks/ songs/ tools/ tools-guide/ data/ plans/

public/content/           コンテンツ（毎日変わる）
├── news.json             GitHub Actions が1日4回更新
├── picks.json            GitHub Actions が1日4回更新（Substack）
└── home.json             手で書く（今日のツール・プロンプト・図鑑・制作メモ）

scripts/
├── collect-news.mjs      ニュース収集
├── collect-picks.mjs     Substack記事の取り込み
├── translate.mjs         英日翻訳
└── feeds.json            28フィードの設定
```

**この分離の利点**：

- **コンテンツ更新にアプリのビルドが要りません**（＝毎日更新できる）
- 機能追加は `features/` にフォルダを1つ足すだけ（＝3〜5年スケールする）

### 技術スタック

| | |
|---|---|
| フレームワーク | React 19 + TypeScript |
| ビルド | Vite 8 |
| スタイル | Tailwind CSS 3 |
| 状態管理 | Zustand |
| ルーティング | react-router-dom（**HashRouter**） |
| アイコン | lucide-react |
| テスト | Vitest（**141件**） |
| **限定コンテンツ配信API** | **Cloudflare Workers + Workers KV**（`worker/api/`） |

**HashRouterを使う理由**：GitHub Pagesのような静的ホスティングで動かすためです。

**`base: './'`**：サブパス公開に対応するため相対パスで出力します。

**サーバーを持たない原則との関係**（✅ 2026-08-08）：
V1憲章の「サーバー・DB・ログイン禁止」は維持しています。
Worker が扱うのは**権限判定と限定コンテンツの配信だけ**で、
**制作データは一切送りません**。曲・歌詞・お気に入り・履歴は従来どおり端末内のみです。

**`server.fs.deny`**（`vite.config.ts`）：
`vite dev` はプロジェクト直下のファイルを配信するため、指定が無いと
`http://localhost:5180/content/premium.json` で限定コンテンツが読めてしまいます。
本番ビルドには元から含まれませんが、**開発中も漏らさない**ために明示的に遮断しています。

### パフォーマンス

制作ツールは `lazy()` で遅延読み込みします。ホームのJSは **約98KB（gzip）** に保っています。

「毎日開きたくなるホーム」を目指す以上、ホームの表示速度は落とせません。

### V1から移植したロジック

**V1のテスト141件が、V2でもそのまま全部通っています。ロジックは1行も書き換えていません。**

移植の手順（今後ツールを増やすときも同じ）：

1. `lib/` をコピー
2. `import` を `deps.ts`（必要な型だけ写した薄いファイル）に付け替え
3. **テストを走らせて、通ることを確認**
4. UIだけ新規に作る

3を先にやることで、UIを書く前に「ロジックが壊れていない」と確定できます。

⚠️ `ai-producer/lib/compare.ts` は実行履歴機能に依存するため**持ち込んでいません**。
履歴機能を作るときに一緒に入れてください。

### 自動化（GitHub Actions）

| ワークフロー | 内容 | タイミング |
|---|---|---|
| `deploy.yml` | ビルドしてGitHub Pagesへ公開 | コードをpushしたとき |
| `update-content.yml` | ニュース・Picksの収集 | 日本時間 6/12/18/22時 |

2つに分けているので、**ニュースが更新されるたびにアプリを作り直す必要はありません**。

---

## 11. ロードマップ

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | プロジェクト基盤・デザインシステム・シェル | ✅ 完了 |
| 2 | コンテンツ基盤＋ニュース機能 | ✅ 完了 |
| 3 | トップページ（制作ファースト） | ✅ 完了 |
| 4 | 制作ツール4種をV1から移植 | ✅ 完了 |
| 5 | 曲一覧・データ管理・Picks・ツール図鑑・プラン画面 | ✅ 完了 |
| 6 | GitHub Pagesへ公開 | ✅ 完了（2026-08-03） |
| 7 | **限定コンテンツのサーバー側アクセス制御** | ✅ **完了（2026-08-08）** |
| 8 | Google/YouTube認証への差し替え | 🚧 **次にやること**（PoC成立検証から） |
| 9 | 決済導入・キー発行の運用 | 🚧 未実装 |
| 10 | V1からの移行（データ移行・切替） | 🚧 未実装 |

### Phase 7：限定コンテンツのアクセス制御（✅ 完了）

**やったこと**：限定コンテンツを公開領域から外し、権限をサーバー側で判定する構成にした。
詳細は §5-A。本番で9項目＋改ざん系6種の遮断を実測確認済み。

**このPhaseで直した不具合3件**（いずれも実機検証で発覚）：

1. 公開JSONの本文を空にしたら、読み込み側が「不備データ」と判断して
   Premiumプロンプトを丸ごと捨てていた（`load.ts` の必須チェック）
2. `usePremiumNote` がセレクタ内で新規オブジェクトを返し、Picksページが無限ループでクラッシュ
3. `vite dev` が `/content/premium.json` を配信していた（本番ビルドは元から安全）

**検証手法の教訓**：`vite preview` とブラウザが `index.html` をキャッシュし、
旧バンドルのまま「本番検証」をしかけた。**再ビルド後は読み込み中のJSファイル名を必ず照合すること。**
`?cachebust=` を付けて読み直すと確実。

### Phase 8：Google/YouTube認証（次にやること）

**着手の前提**（§12 未決定事項 1〜3）が未達のため、まだ実装に入れません。

1. **PoC成立検証** — 会員CSVに照合可能なチャンネルIDが含まれるか。
   **カラスイさんのGoogleログインが必要**（代行不可）
2. **`youtube.readonly` のスコープ区分** — 「制限付き」なら毎年のCASA監査（有料）が発生
3. **独自ドメインの取得** — OAuth審査の前提条件

差し替え箇所は `resolveMembership()` の `mode: 'youtube'` のみ（§5-A）。

### Phase 9 以降でやること

- ~~`PlansPage.tsx` のライセンスキー入力欄を `/api/session` 呼び出しへ差し替え~~ ✅ 完了（2026-08-08）
- セッション期限切れ（30日）後の**自動**再ログイン導線（同じキーの手動再入力は可能。
  期限の事前表示は2026-08-10に追加済み）
- 決済導入（Stripe など）とキー発行の運用フロー
- Premium限定ツールの中身を決めて実装
- 制作テンプレートの実装

---

## 12. 未決定事項

❓ **勝手に決めずに、必ず確認してください。**

| # | 項目 | 補足 |
|---|---|---|
| # | 項目 | 補足 |
|---|---|---|
| 1 | **🔴 案A' の成立検証（CSVにチャンネルIDがあるか）** | **最優先・これが決まるまで実装しない。**手順は §5「成立条件の検証手順」。検証には `scripts/poc-check-member-csv.mjs` を使う。カラスイさんの作業（15分） |
| 2 | **🔴 `youtube.readonly` は「機微」か「制限付き」か** | 制限付きなら**有料のCASA監査が毎年必要**になる。Google Cloud Console の「スコープを追加または削除」画面のラベルで1分で確認できる。→ §5「OAuth審査」 |
| 3 | **🔴 独自ドメインの取得** | **OAuth審査の前提条件。**`github.io` はGitHubの所有ドメインで、Search Console で所有権を検証できないため審査を通せない見込み。以前は「あれば良いもの」だったが**必須に格上げ** |
| 4 | **案A'' （目視承認方式）を採るか** | 案A'が不成立だった場合の代替。照合キーは検証済みチャンネルIDのままで、突き合わせだけ人が行う。→ §5「案A''」 |
| 5 | **CSV同期の頻度と自動化の範囲** | 案A'成立時のみ。週1回以上を推奨（30日超で赤警告） |
| 6 | **プライバシーポリシー／削除案内のURL** | 独自ドメイン取得後に `/privacy` などへ設置。OAuth審査で必須 |
| 7 | **マイページを作るか** | 現在は「マイスタジオ」グループで代用。独立ページを作る場合、何を載せるかも未定 |
| 8 | ~~**AI秘書をどうするか**~~ | ✅ **2026-08-10 解決。** V1から移植して実装（Premium以上）。「今日試すAIツール」が担うのは"新しいものを知る"導線で、秘書が担うのは"自分の止まっている曲の次の一歩"——役割が重ならないと判断した |
| 9 | **決済手段** | Stripe導入の時期とキー発行の運用フロー |
| 10 | **Premium限定ツールの中身** | 「Premium限定ツール」として何を作るかが未定 |
| 11 | **制作テンプレートの中身** | 同上 |
| 12 | **V1からのデータ移行方式** | 最低限でよい。将来対応で十分という合意 |
| 13 | **Master 30名に達したときの運用** | 待機リストを作るか、募集を止めるか。**発行数は機械的に止まらない**ので台帳が要る |
| 14 | **ニュースの「詳細版」の中身** | Premium特典の「AI音楽ニュース詳細版」が具体的に何かが未定 |
| 15 | **Discordロールの自動付与** | 認証とは分離して将来対応。Discordを**認証に使う案Bは不採用で確定** |
| 16 | **制作メモの補充運用** | 記事はRSSで自動更新、メモは手書き。**最新記事にメモが無い状態が常態化**する。誰がいつ書くか（→ §7） |
| 17 | **ライセンスキーの配布・管理台帳** | 誰にどのキーを渡したか、失効をどう扱うか。Master 30名上限の管理と一体 |
| 18 | **セッション期限切れ後の“自動”再ログイン導線** | 2026-08-10に30日へ延長し、プラン画面に期限を事前表示するようにした（部分対応）。切れた後の自動更新（リフレッシュトークン等）は未実装で、同じキーを手で入れ直す必要がある |
| 19 | ~~**`.env.production` の管理**~~ | ✅ **2026-08-11 解決。** コミット対象に含めた（値は `VITE_API_BASE` という公開URLのみで秘密情報ではない）。本番ビルドにAPIのURLが焼き込まれていることを配信JSで実測済み |
| 22 | ~~**Premium / Master / revoked の本番実キー検証**~~ | ✅ **2026-08-12 完了。** カラスイさん本人へのMasterキー発行（M-007）で確認した。プラン画面で「Studio Masterになりました」「いまの権限：Studio Master（YouTubeメンバーシップ経由）」「有効期限2026/9/11」「Master限定Discord案内カード」を実機確認。<br>**途中で本番の`LICENSE_PEPPER`不一致という初めての実運用障害を発見・解消した**（→ §5-A・変更履歴・軍配DECISIONS.md参照）。M-005・M-006は失効し、pepperを`wrangler secret put`で再設定してからM-007を発行した |
| 20 | ~~**検証用ライセンスキーの失効**~~ | ✅ **2026-08-10 再確認済み。** 本番KVの `serial:*` は `M-002`・`P-003` の2件のみで、いずれも `license:*` レコードを直接確認し `revoked:true` を確認した。有効なキーは0本 |
| 21 | **PoC の残骸（Cloudflare側）の削除** | 🟡 **コード側は2026-08-11に削除済み**（`src/features/auth-test/`・`worker/auth-test/`・`/auth-test` ルート・PoC用スクリプト3本・`vite.config.ts` のプロキシ設定）。**Cloudflare上の `studio-next-auth-test` Worker と PoC専用KV名前空間はまだ残っている。** 本番公開が正常に完了したことを確認してから、別タスクとして削除する（カラスイさんの指示・2026-08-11）。<br>削除しても本実装には一切影響しない（`worker/api` は別Worker・別KV）。段名→Role対応表 `scripts/membership-levels.mjs` は手動発行の参照表として**意図的に残している** |

### ✅ 決定済みになった項目

| 日付 | 項目 | 決定 |
|---|---|---|
| 2026-08-04 | 正式な権限認証の方式 | **案A（Google OAuth + YouTube Data API 直接連携）に決定。案Bは不採用** |
| 2026-08-04 | V1憲章「ログイン禁止」との整合 | **認証処理に限り例外を認める。制作データを送らない原則は維持** |
| 2026-08-04 | Partner Manager／担当者の有無 | **付いていない。**YouTube／Googleからの直接連絡や定期的な支援は受けていない → **案Aの `members.list` は当面使えない** |
| 2026-08-04 | 案A' を先に作るか | **作らない。**CSVに照合可能なチャンネルIDが含まれることを検証してから判断する |
| 2026-08-04 | セッション更新方式 | **`prompt=none` への依存をやめ、2トークン方式（24時間＋90日）に変更** |
| 2026-08-04 | 表示名での照合 | **禁止。**一意でも不変でもないため、なりすましを許す |
| 2026-08-04 | Premium限定コンテンツのWorker配信化 | **認証実装と同時に実施。**公開JSONにはメタデータのみ残す |
| **2026-08-08** | **Worker配信化を認証より先に実施** | 認証（Google/YouTube）は外部要因で着手できないが、**コンテンツ保護は独立して先に実施できる**と判断。先行して本番稼働 |
| **2026-08-08** | **限定コンテンツの置き場所** | **Cloudflare KV `premium:content`。**Workerに埋め込まない（再デプロイやCIで消えるため） |
| **2026-08-08** | **KV名前空間** | **`STUDIO_API_KV` を新規採番。**PoC用とは分離（PoC削除時の巻き添えを防ぐ） |
| **2026-08-08** | **セッション方式** | **HS256・24時間の単一トークン。**当初のRS256/2トークン案は不採用（フロントに検証させない方針のため共通鍵で足りる） |
| **2026-08-08** | **本番でのlocalStorage** | **権限の根拠として信用しない。**初期値は常に `free`、変更経路は検証済みセッションのみ |
| **2026-08-08** | **ライセンスキー方式の位置づけ** | **暫定。**本番で認可の仕組みを検証するためのもので、最終的にGoogle/YouTube認証へ差し替える |
| **2026-08-08** | **既出3件の限定コンテンツ** | **「すでに公開済み」として扱う。**Git履歴の書き換えは行わず、内容を新しいものに差し替える |
| **2026-08-08** | **Channel Memberships API（案A）** | **🔴 不採用が確定。**実機で `members.list`・`membershipsLevels.list` とも **403 forbidden**。両スコープ承認済み・運営者チャンネルとして認証済みでの403のため、設定不備ではない。Partner Manager が付くまで使えない |

---

## 13. 用語集

| 用語 | 意味 |
|---|---|
| **V1 / Version 1** | 既存Studio（`~/AI Music Studio`）。本番稼働中。**触らない** |
| **V2 / Version 2** | このプロジェクト（`~/AI音楽部 Studio Next`） |
| **Role** | 権限。`free` / `premium` / `master` |
| **RoleSource** | 権限の入手経路。`none` / `youtube` / `studio` |
| **カラスイ Picks** | Substackの記事をStudioに表示する機能。旧称「AI音楽部 Picks」 |
| **制作メモ** | Picks記事ごとのStudio限定の補足。Premiumの価値の中心 |
| **限定コンテンツAPI** | `worker/api/`。権限を検証して限定本文を返す Cloudflare Worker（§5-A） |
| **`resolveMembership()`** | 権限判定の唯一の場所。ここを通らない role は存在しない |
| **セッショントークン** | APIが発行するHS256署名付きJWT。30日有効（2026-08-10延長）。**本番で権限を示す唯一のもの** |
| **`lastLoginAt`** | ライセンスKV記録の項目。最後にログインが成功した日時。有効期限の目安計算に使う |
| **`contentLoaded`** | `/api/health` の項目。限定コンテンツがKVに入っているかの確認に使う |
| **暫定方式（ライセンスキー）** | 運営者が発行するキーによる認可。Google/YouTube認証までのつなぎ |
| **続きから** | 前回さわったものへ戻るホームのブロック |
| **AI音楽部 Lab** | Master限定のDiscordコミュニティ |

---

## 14. 変更履歴

| 日付 | 内容 |
|---|---|
| **2026-08-19** | **Studio Creator（3,490円）を新設し、プラン体系を4段階に変更。** ①`Role` を `free < premium < creator < master` の**継承型4段階**に拡張（フロント`role.ts`・Worker`types.ts`の二重定義を両方）。上位は下位を必ず含むため、**既存Masterユーザーが失う機能は無い**。既存ユーザーのroleも書き換えていない（キーは1人1本で発行済みのものがそのまま有効） ②権限の上下比較を`ROLE_RANK`／`satisfiesRole()`に一本化。以前はPlansPage・ToolsPage・Workerがそれぞれ独自の順序表を持っており、プランが増えるたびに直し忘れる形だった。`loadRole()`の許可リストも個別列挙をやめて`ROLE_RANK`参照に変更（`creator`が漏れていて開発時にリロードすると権限が落ちるバグを修正） ③**Creator限定機能はSeedance Batch Studioとシーダンス2.5 プロンプト工房の2つ。** 2026-08-12まではMaster限定だったが、Creatorの目玉としてCreator以上へ引き下げた。Masterの差別化はDiscord（人が関わる伴走）に一本化 ④**「Studioのプラン名」と「YouTubeの商品名」を分離。** Studio内は`Studio Creator`（AI音楽部を付けない・`ROLE_LABEL`）、YouTubeの加入案内だけ`AI音楽部 Studio Creator`（`YOUTUBE_TIERS[].youtubeName`）。従来は`name`1本で、しかもPremium/Masterの表示名はYouTube側の実名と食い違っていた ⑤YouTube側でPremium/Masterが改名されたのを反映（`AI音楽部 Studio Premium` / `AI音楽部Studio Master`）。旧名は`membership-levels.mjs`の`aliases`へ退避（改名前の申請控えと突き合わせるため）。**段名は実画面の表記をそのまま写す**方針を明記した——`Ai大好き部`（AIではない）、Masterだけ「部」と「Studio」の間に空白が無い、など不揃いだが、揃えて書くと照合が外れて全員freeへ倒れる ⑥料金ページを4カード化。`lg:grid-cols-3`のままでは4枚目が余るため、スマホ1列→タブレット2×2→`xl`で4列と段階的に広げるレイアウトへ変更 ⑦ロック時の文言を「◯◯限定です」から「**Studio Creatorで利用できます**」へ。アップグレード手順のYouTube段名ハードコードも`role.ts`参照に変更（旧名のまま古くなっていた） ⑧**キー発行まわりの4段階対応**：`issue-license-key.mjs`が`premium|master`しか受け付けず**Creatorキーを発行できない状態だった**のを修正。台帳の連番プレフィックスに`C`を追加（`license-ledger.mjs`の`PREFIX`・行パターン・集計）、`revoke`／`check-license-expiry`の連番パターンも`[PCM]`へ拡張 ⑨型チェック・テスト180件・本番ビルド・Worker dry-run、free/premium/creator/masterの4権限とPC/タブレット/スマホの表示を実機確認 |
| **2026-08-12（続報4）** | **初回実ユーザー（カラスイさん本人）へのMasterキー発行で、`LICENSE_PEPPER`不一致の障害を発見・解消。未決定事項#22を完了。** ①M-005発行→プラン画面で「確認できませんでした」。M-006を再発行しても同症状。KV記録・Worker健全性に問題は無く、2回とも同じ結果だったことから手入力ミスではなく構造的原因と判断 ②原因は、発行スクリプト実行時に手入力していた`LICENSE_PEPPER`と、本番Workerに`wrangler secret`で設定済みの値の食い違い。ハッシュ照合は1文字でもずれれば必ず不一致になる ③M-005はカラスイさんが送ってくれたスクリーンショットに実際のキー文字列がそのまま写っていたため、実害の有無に関わらず予防的に失効。M-006も切り分けのため失効 ④新しいpepperを`crypto.randomBytes`で生成し`wrangler secret put`で本番へ設定、同じ値を直後の発行コマンドに使い回すことで手入力のズレを無くし、**M-007**を発行 ⑤プラン画面で「Studio Masterになりました」「有効期限2026/9/11」「Master限定Discord案内カード」を実機確認。**Premium/Master/revokedの本番実キー検証がここで完了**（revoked確認はM-005・M-006の失効自体がそのまま実例になった：失効後にそのキーで新しいセッションを発行できないことを、2回目・3回目の発行がそれぞれ独立したKVレコードであることから間接的に確認） ⑥教訓として§5-Aに「pepperは1箇所に確定保存し、発行のたびに手入力しない」を明記 |
| **2026-08-12（続報3）** | **第3テーマの配色とテーマ名を修正。** ①配色を「紫〜マゼンタ」から**「炭×琥珀×藍鼠」**（シーダンス2.5プロンプト工房の編集室UIと同じ考え方）へ全面的に作り直した。背景`hsl(225 17% 9%)`・primary`hsl(37 74% 56%)`（琥珀）など、実際のhexからHSLを算出して`.theme-suno`（当時の名称）の変数を差し替え ②**テーマ名を「Suno風」から「琥珀」へ変更。** 他社サービス名をテーマ名に使うのは公式提携・模倣と誤解されるもとなので避けるべき、との指摘を受けての対応。`Theme`型の値も`'suno'`→`'amber'`、CSSクラスも`.theme-suno`→`.theme-amber`に変更（保存済みの`'suno'`という古い値は3値のどれとも一致しないため、次回起動時は端末設定に自動で戻る＝実害なし）。アイコンもPalette→Flame（炎）に変更 ③型チェック・テスト180件・本番ビルド・実機確認すべて成功 |
| **2026-08-12（続報2）** | **表示テーマにSuno風を追加し、ライト/ダーク/Suno風の3択にした。** `useTheme.ts`のThemeを`'light'\|'dark'\|'suno'`に拡張。Suno風は`index.css`の`.theme-suno`でCSS変数（`--primary`等）だけを上書きし、`.dark`クラスも併用することでV1由来の`dark:`ユーティリティ（emerald色の微調整など4箇所）もそのまま効かせる設計にした。**配色はSunoの公式カラーコードではなく、ほぼ黒の背景＋紫〜マゼンタの強いアクセントという雰囲気だけを寄せた独自パレット**（商標・見た目の直接模倣を避けるため）。Topbarのテーマボタンをトグルからドロップダウン（ライト/ダーク/Suno風を選択・現在値にチェック表示）に変更。型チェック・テスト180件・3テーマの実機確認すべて成功 |
| **2026-08-12（続報）** | **Studio直販の廃止・外部ツールの並び替え。** ①**Studio直販を廃止し、加入経路をYouTubeメンバーシップの1本に統一。** きっかけはプラン画面の価格表示（大きい¥980＝Studio直販／小さい¥1,290＝YouTube経由の二重表示）への疑問。調べたところ、Studio直販は決済導線が一度も実装されておらず表示だけの存在で、しかも実装面でも実際に発行されるセッションの`source`は常に`license`（＝YouTube会員確認を経て発行）で`'studio'`という区分がUIに現れる経路は構造的に無かった。存在しない経路の表示は誤解のもとなので、`STUDIO_PRICE`定数と`RoleSource`の`'studio'`を削除し、価格はYOUTUBE_TIERSの1本（Premium 1,290円・Master 5,600円）に統一。`role.ts`・`api.ts`（signIn/refreshが返すsourceを常に`'youtube'`に）・`PlansPage.tsx`（二重価格表示・「Studio直販」ラベル・「StudioとYouTubeのどちらからでも」の文言を削除）・PROJECT_SPEC.md／README.md／docs/設計方針.mdを更新 ②Masterの「対象」表記が§4の一覧表にだけ「仕事・収益化につなげたい人」のまま残っていたのを見つけて是正（8/11の説明文修正時に見落としていたもの） ③`ToolsPage.tsx`の外部ツール順で、字幕自動生成ツールとマスタリング自動生成ツールの位置を入れ替え（要望のみ、理由の記載なし） ④型チェック・テスト180件・本番ビルド・free/Premium/Masterの3状態実機確認すべて成功 |
| **2026-08-12** | **Seedance2.5プロンプト工房をMaster限定の外部ツールとして追加。** ①ツール本体（`~/シーダンス2.5プロンプト工房/`。フォルダ名は日本語のまま、Studio側の表示名のみ英語表記に統一）はSeedance 2.5のプロンプトを香盤表形式で組み立てる静的PWA。生成APIは叩かず、正しい記法のプロンプト文字列を組み立ててコピーさせるだけの設計。公開前は未追跡（どのGitにも入っておらず、公開URLが存在しない）状態だったため、まず `lofi-detective-website` リポジトリ（既存の絵コンテ・スタイルプロンプト工房等と同じ公開先）のサブフォルダ `seedance-prompt-koubou/` として追加してからStudio側の実装に進んだ ②git remoteに壊れたプレースホルダ認証情報が埋め込まれていたため`gh auth setup-git`の認証情報に一本化して修正。ローカルの`ローファイ探偵HP`クローンは253コミット遅れていたため、作業ツリーがcleanであることを確認してから安全にfast-forwardしてから作業 ③push後の初回Jekyllビルドは`SSL_connect...self-signed certificate`というActionsランナー側の一時的なネットワークエラーで失敗（コンテンツとは無関係）。Pages Build API（`POST /pages/builds`）で再ビルドして解消し、実際にHTTP 200で配信されることを実測 ④`ToolsPage.tsx`の`EXTERNAL_TOOLS`にSeedance Batch Studioの直後として追加。**requires: 'master'**（Seedance Batch Studioと同じ動画生成の文脈のため同枠に揃えた）。他の7つ（V1由来）とは異なりV1に対応が無い新規追加である旨をコード内コメントに明記 ⑤free/Premium/Masterの3状態を実機確認。Premiumではロックカード→展開で他のMaster限定ツールと同じ4ステップのアップグレード案内が出ることを確認。型チェック・テスト180件・本番ビルドすべて成功 |
| **2026-08-11（続報3）** | **Premium→Masterのアップグレード導線を追加し、Masterの説明文を実態に合わせた。** ①`role.ts` に **`OFFICIAL_LINE_URL`** を新設。Discordの招待URL（定員のある私的コミュニティの鍵）とは性質が違い、既にYouTubeメンバー限定投稿で公開案内している問い合わせ窓口のため、`YOUTUBE_JOIN_URL` と同じ扱いで定数化してよいと判断（カラスイさんの確認済み） ②`PlansPage.tsx` のMasterカードに、**Premiumの人が見ているときだけ**「Masterにアップグレードする」＋公式LINEボタンを追加。free・Master本人には出さない ③新規Masterキー発行→本人送付→旧Premiumキー失効、という運用に統一。**既存キーのrole書き換えはしない**（台帳から履歴が追えなくなるため）。ダウングレード（Master→Premium）も同じ考え方。§5-Aに運用手順を新設 ④`issue-license-key.mjs`・`revoke-license-key.mjs`・`license-ledger.mjs` に**任意の `--note`** を追加し、「PremiumのP-005から変更」のような移行履歴を台帳の備考に残せるようにした。省略時は従来どおり（後方互換）。ローカルKVで発行→失効の往復を実測し、台帳の7列フォーマットが壊れないことを確認 ⑤**Masterの説明文を修正**：「AI音楽を仕事や収益化につなげたい人」→「AI音楽をもっと深く学びたい人。作品を見てもらいながら、制作力を伸ばしたい人。」実際に提供していない成果（仕事・収益化）を訴求しないため。コード内を検索し、他に同趣旨の表現がPlansPage.tsx以外に無いことを確認済み |
| **2026-08-11（公開）** | **🚀 認証システムとAI秘書を本番公開した。** 実施順序は「コミット → Worker再デプロイ → Worker実測 → push → Actions確認 → 本番実測」。①**8コミット・53ファイル**を機能単位で分割してpush（限定コンテンツ遮断／認可API／権限判定の一本化／コンテンツ分離／AI秘書／画面の出し分け／運用スクリプト／仕様書）。auth-testは一度もコミットされていなかったため削除の差分は生じず、結果 `vite.config.ts` の差分は `fs.deny` だけ、`main.tsx` はHEADと完全一致に戻った ②**Worker再デプロイ**（版 `4fca2def`）でTTLが24時間→**30日**に。限定コンテンツはKVに置いてあるためデプロイ前後で件数・`updatedAt` とも完全一致（Workerに同梱していたら消えていた） ③**GitHub Actions「公開」成功**（npm ci／npm test 180件／npm run build／deploy-pages）。ニュース自動更新Botが23コミット先行していたが `news.json` のみで重複ゼロだったためrebaseで直線化 ④**本番実測**：配信バンドルが `index-DdyeB-vp.js` → `index-DLgoBiON.js` に更新され、**Worker APIの参照が入った**（公開前は0件＝認証システムが本番に無かった状態が解消）。**Discord招待URLが配信JSから消滅**（公開前は埋め込まれていた）。auth-test／OAuthクライアントID／DevRoleSwitcherいずれも0件。`content/premium.json` は404、公開JSONにも限定本文なし。free状態で無料6ページは通常表示・制作系4ページはロック画面・`/auth-test` は404 ⑤**Premium／Master／revoked の実キー検証は意図的に省略**し、初回の実ユーザー発行時に行う（→ 未決定事項#22）。本番の `LICENSE_PEPPER` を取得・表示・保存しない方針のため。代わりに同一コードで `iat`/`exp` の差が2,592,000秒＝ちょうど30日であることを実測した ⑥有効な検証用ライセンスキーは**0本**（`M-002`・`P-003` の2件のみで、いずれもレコード本体で `revoked:true` を確認） |
| **2026-08-11** | **本番公開前の最終整理（pushはまだしていない）。** ①**セッションTTLの食い違いを実測で確定。** 仕様値は**30日**だが、本番Workerの最終デプロイが2026-08-08で、**本番で実際に動いているのは24時間**であることが判明（`wrangler deployments list` で実測）。`lastLoginAt` と `/api/me` の `exp` 返却も同様に未デプロイ。→ §5-A に「Workerの再デプロイが必要」を明記し、公開手順に組み込んだ ②**35,000円「企業様向け」の除外を PROJECT_SPEC.md §4 に明文化**（従来は `membership-levels.mjs` と軍配の判断ログにしか無かった）。価格順の自動昇格を禁じる理由も併記 ③**Discord旧招待URLを公開コードから完全削除**（`README.md`・`PROJECT_SPEC.md` に残っていた分）。あわせて「Initial commit からGit履歴と本番配信JSに含まれていた」事実を §6 に記録。**コードから消しても取り消せないため、Discord側での失効が必要**（カラスイさんの作業） ④**auth-test PoC をコードから削除**（`src/features/auth-test/`・`worker/auth-test/`・`/auth-test` ルート・`main.tsx` のOAuthコールバック・`vite.config.ts` のプロキシと `allowedHosts`・PoC用スクリプト3本・`.gitignore` と `.env.local.example` の該当行）。**`vite.config.ts` の `fs.deny` は限定コンテンツ保護のため残した** ⑤`scripts/membership-levels.mjs` は削除せず、用途を「手動ライセンス発行・将来CSV照合の参照表」に書き換え。段名を `current`（実画面で確認した現行表記）と `aliases`（表記ゆれ・旧称）に整理し、`node scripts/membership-levels.mjs "段名"` で引ける確認CLIを追加。未知の段は従来どおり free に倒す ⑥`.env.production`（本番APIのURLのみ・秘密情報なし）をコミット対象に含める方針を確定 |
| **2026-08-10（続報2）** | **AI秘書をV1から移植して完成（未決定事項#8を解決）。** ①`src/core/secretary/`（types / message / leveling / themes / store / avatar）と `src/features/secretary/`（設定画面・ホームのカード・アバター）を新設し、`/secretary` のプレースホルダを置き換え ②**AIには一切問い合わせない**。文章は端末内のルールだけで組み立てる（V1と同じ思想） ③V2の曲データに合わせて移植内容を調整：`sunoPrompts[]`→`sunoPrompt`、`mvPrompts[]`→`mvPrompt`、`youtube.url` は存在しないので `status==='mv'` を公開待ちとみなす、レベルの母数は `completedAt` ではなく **`status==='published'` の曲数** ④V1は公開済み曲に「批評ツール」を名指しで勧めていたが、V2では同ツールがMaster限定になったため**ツール名を出さず「振り返り」だけを勧める**形に変更（表示と実態の食い違いを作らないため） ⑤**権限はPremium以上**。ホームのカードも有料プランのときだけ描画する ⑥`core/storage/streak.ts` に **`currentStreak()`**（記録を足さずに読むだけ）を追加。起点を今日または昨日に許し、ホーム未経由の日に連続日数が0に見える問題を防ぐ ⑦`/data` の書き出し・読み込みに**秘書の設定・節目・画像（data URL）を追加**。端末を変えて秘書が初期化されるのでは「持ち出せる」と言えないため ⑧テストを39件追加（141→180件） |
| **2026-08-10（続報）** | **外部ツールの残り3種を追加・スタイルプロンプト工房を無料開放。** ①**ギターコードTAB・マスタリング自動生成ツール**をPremium以上として追加（絵コンテと同枠） ②**スタイルプロンプト工房は無料開放**に変更。他所（`lofi-detective-website`）で元から無料公開済みのツールのため、Studio側で有料の壁にする理由がないという判断 ③無料開放に伴い `/tools` ルートを `PremiumGate` の対象から外し、`ToolsPage.tsx` 内部で無料/Premium/Masterごとの出し分け（ロックカード）を行う方式に変更。無料ユーザーは制作ツール本体（歌詞レビュー等）とMaster専用・Premium専用の外部ツールをロック済みカードとして閲覧でき、そこからアップグレード手順を確認できる ④**「鍵ツールはなるべく下に揃える」デザイン**を追加。外部ツール一覧はロック中のものが自動的に配列末尾に回るよう表示順を並べ替え ⑤ツール名・説明文・URLはV1 `src/lib/constants.ts` の `EXTERNAL_TOOLS` と完全一致させた（「絵コンテ生成ツール」→「絵コンテツール」に是正） |
| **2026-08-10** | **セッション30日化・外部ツール4種の権限決定・セキュリティ検証。** ①セッションTTLを24時間→**30日**に延長（`session.ts`）。理由：24時間だと再ログイン導線が未実装のため無言でロックに戻る体験になっていた ②ライセンスKV記録に **`lastLoginAt`** を追加。ログイン成功のたびに書き戻す ③**`scripts/check-license-expiry.mjs`** を新設。退会対応時に「実際いつまで有効か」を連番指定で確認できる ④プラン画面に**有効期限を事前表示**（同じキーの再入力で延長できる旨も明記） ⑤外部ツール4種の権限を決定：**絵コンテ生成ツールはPremium以上**でStudio内から直接開けるボタンとして表示。**字幕自動生成・楽曲批評・Seedance Batch StudioはMasterのみ**。Premiumには鍵アイコン→タップでMasterへのアップグレード手順（YouTube変更→公式LINEでチャンネル名とプラン申告→確認後キー発行）を表示 ⑥**セキュリティ検証**：価格/role偽装・トークン改ざんの3攻撃すべて失敗を確認。role判定に「価格」という概念がコードに存在しないため。残る真のリスクは運用遅延のみ（③④で緩和） ⑦ギターコードTAB・マスタリング自動生成・スタイルプロンプト工房は今回も対象外のまま |
| **2026-08-08** | **限定コンテンツのアクセス制御を実装し、本番稼働（§5-A 新設）。** ①Premium限定の本文を公開JSONから分離し、**Cloudflare KV `premium:content`** へ移動。`public/` にはロック表示用のメタ情報だけを残す ②認可API `worker/api/` を実装・デプロイ（`https://studio-next-api.karasui1969.workers.dev`）。**未認証401・権限不足403で、本文そのものを返さない** ③権限判定を **`resolveMembership()` 1箇所に集約**。クライアントの申告する role は一切信用しない ④**本番では localStorage を権限の根拠にしない**（初期値は常に free） ⑤DevRoleSwitcher は開発環境のみ。**二重の安全装置**（`import.meta.env.PROD` ＋ Worker側 `DEV_ROLE_OVERRIDE`） ⑥セッションは **HS256・24時間**（当初のRS256/2トークン案から変更） ⑦KVは **`STUDIO_API_KV`** を新規採番しPoC用と分離 ⑧限定コンテンツの投入は `scripts/push-premium-content.mjs`。**`contentLoaded` で反映確認**、空ファイル投入は中止する ⑨`vite dev` の `/content/premium.json` 配信を `server.fs.deny` で遮断 ⑩**ライセンスキー方式は暫定**と明記。Google/YouTube認証への差し替え口を維持 |
| 2026-08-04 | **設計を改訂（実装は未着手）。** ①Partner Managerが付いていないことが確定し、案Aの `members.list` は当面使えないと判明 ②案A'は**成立未確認**として実装保留。判定用に `scripts/poc-check-member-csv.mjs` と検証手順を用意 ③**表示名での照合を禁止**に ④セッション更新を `prompt=none` 依存から**2トークン方式（24時間＋90日）**へ変更。ユーザーのGoogle refresh tokenは取得も保存もしない ⑤CSV取り込み機能を設計（CSVはサーバーへ送らずブラウザ内で処理・ハッシュのみ保存・版管理でロールバック可） ⑥**Premium限定コンテンツのWorker配信化を認証と同時実施**に決定。Git履歴に残る問題も明記 ⑦OAuth審査の要件を特定。**独自ドメインが審査の前提条件**であることが判明 |
| 2026-08-04 | 認証方式を**案A（Google OAuth + YouTube Data API）に決定**。V1憲章の例外を認証処理に限って承認。§5に詳細設計（OAuthフロー/チャンネルID取得/members.list照合/レベルID対応/refresh token/JWT/退会処理/エラー案内/仮実装の削除手順）を追記。調査により**`members.list` はPartner Manager経由でしか権限を得られない**ことが判明し、橋渡し構成「案A'」を併記。あわせて**Premium限定コンテンツが公開JSONに平文で入っている**問題を記録（実装は未着手・承認待ち） |
| 2026-08-03 | GitHub Pagesへ本番公開。`YOUTUBE_JOIN_URL`（メンバーシップ加入画面への直接リンク）を確定し `role.ts` に一元化 |
| 2026-08-02 | 仕様書を新規作成。価格を確定（Studio直販 Premium 980円 / YouTube 1,290円の併存、AI大好き部690円を追加） |
| 2026-08-01 | 権限をRoleベースに再実装。ホームを制作ファーストで全面再設計。Substack連携を実装。制作ツール4種の移植完了 |
| 2026-07-31 | プロジェクト開始。設計方針を策定 |
