# AI音楽部 Studio — プロジェクト仕様書

**このファイルが唯一の正（Single Source of Truth）です。**
実装を変更したら、必ずこのファイルも同じコミットで更新してください。
ここに書かれていないことは「決まっていない」と判断してください。

| | |
|---|---|
| 最終更新 | 2026-08-02 |
| バージョン | Version 2（開発中・未公開） |
| リポジトリ名 | `studio-next` |
| 公開予定URL | `https://karasui1014.github.io/studio-next/` |
| ローカル起動 | `npm run dev` → http://localhost:5180 |

### 記号の意味

このファイル全体で、次の3つを区別しています。

| 記号 | 意味 |
|---|---|
| ✅ **決定** | 合意済み。実装済み、または実装方針が固まっている |
| 🚧 **未実装** | 決定済みだが、まだコードになっていない |
| ❓ **検討中** | まだ決まっていない。勝手に決めずに確認すること |

---

## 0. 新しく開発を引き継ぐ人へ

### まず読む順番

1. このファイル（全体像）
2. `docs/設計方針.md`（なぜそう決めたかの経緯）
3. `README.md`（動かし方・公開手順）

### 最初にやること

```bash
cd "~/AI音楽部 Studio Next"
npm install
npm run dev     # http://localhost:5180
npm test        # 141件通れば健全
```

### 絶対に守ること

1. **既存Studio（Version 1、`~/AI Music Studio`）を絶対に変更しない。**
   これは別プロジェクトです。V1は本番稼働中なので触りません。
2. **ユーザーの制作データを外部に送らない。** サーバー・DB・ログインを持ちません。
   すべて端末内（localStorage）に保存します。
3. **データの書き出しは無料のまま維持する。** ここを有料化するとデータの人質化になります。
4. **権限は価格ではなく Role で判定する。** 画面側は必ず `useRole()` を通します。

### 迷ったときの判断基準

> **Studioは「読む場所」ではなく「作る場所」。**
> ユーザーが開いた瞬間に思ってほしいのは「今日は何を作ろう」であって、
> 「今日はどんなニュースがあるだろう」ではない。

機能を足すか迷ったら、これに沿うかで判断してください。

---

## 1. プロジェクト概要

### 正式名称

| 項目 | 値 |
|---|---|
| サービス名 | **AI音楽部 Studio** |
| サブタイトル | **AI音楽クリエイターのホーム** |

✅ **決定**：この2つは全画面・全ドキュメントで統一します。表記ゆれを作らないこと。

### コンセプト

AI音楽クリエイターが**毎日最初に開くホーム画面**。

ニュースサイトではありません。毎日開いて制作したくなるStudioです。
ニュースは「毎日来る理由」であって「居る理由」ではありません。
居る理由は、自分の制作がそこにあることです。

### ターゲット

| 層 | 状態 | Studioでの体験 |
|---|---|---|
| これから始める人 | AI音楽に興味があるが未着手 | 無料で情報とツールに触れる |
| 日常的に作る人 | 週に何曲か作っている | Premiumで制作環境を上げる |
| 仕事にしたい人 | 収益化・作品性を上げたい | Masterで人に見てもらう |

### 成功指標

✅ **決定**：訪問数ではなく **連続利用日数（継続してStudioを開いた日数）**。
この指標に効かない機能は後回しにします。

### 4つの場所の役割分担

✅ **決定**：それぞれの役割を混ぜないこと。

| 場所 | 役割 |
|---|---|
| YouTube | **学ぶ**（出会いの入口） |
| Substack | **記事を読む** |
| Discord（AI音楽部 Lab） | **交流する** |
| **Studio** | **毎日制作する** |

理想の流れ：

```
YouTubeで新しいAIツールを紹介
    ↓
詳しい検証や考察はSubstackに書く
    ↓
Studioに「カラスイ Picks」として自動表示される
    ↓
Premium会員はStudio内で制作メモや関連プロンプトにアクセス
```

一つ記事を書くたびに、YouTube・Substack・Studioの3つが同時に育つ設計です。

---

## 2. サービス全体構成

### 画面一覧と実装状況

| 画面 | ルート | 実装 | 実装ファイル |
|---|---|---|---|
| ホーム | `/` | ✅ 完了 | `src/features/home/HomePage.tsx` |
| AI音楽ニュース | `/news` | ✅ 完了 | `src/features/news/NewsPage.tsx` |
| カラスイ Picks | `/picks` | ✅ 完了 | `src/features/picks/PicksPage.tsx` |
| AIツール図鑑 | `/tools-guide` | ✅ 完了 | `src/features/tools-guide/ToolsGuidePage.tsx` |
| 制作ツール一覧 | `/tools` | ✅ 完了 | `src/features/tools/ToolsPage.tsx` |
| ├ 歌詞レビュー | `/tools/lyrics-review` | ✅ 完了 | `src/features/tools/lyrics-review/` |
| ├ MVアイデア | `/tools/mv-idea` | ✅ 完了 | `src/features/tools/mv-idea/` |
| ├ AIプロデューサー | `/tools/ai-producer` | ✅ 完了 | `src/features/tools/ai-producer/` |
| └ ストーリーボード | `/storyboard` | ✅ 完了 | MVアイデアと同じ画面 |
| プロンプト工房 | `/prompts` | ✅ 完了 | `src/features/tools/prompt-dex/` |
| 曲一覧 | `/songs` | ✅ 完了 | `src/features/songs/SongsPage.tsx` |
| 曲詳細 | `/songs/:songId` | ✅ 完了 | `src/features/songs/SongDetailPage.tsx` |
| データ管理 | `/data` | ✅ 完了 | `src/features/data/DataPage.tsx` |
| プラン | `/plans` | ✅ 完了 | `src/features/plans/PlansPage.tsx` |
| マイページ | — | 🚧 未実装 | 下記参照 |
| AI秘書 | `/secretary` | 🚧 未実装 | プレースホルダのみ |

**マイページについて** ❓ **検討中**
現在は「マイスタジオ」というナビゲーショングループ（AI秘書・データ管理・プラン）で
代用しています。独立した「マイページ」を作るかどうかは未決定です。
作る場合、何を載せるか（制作統計・連続日数・保存データの概要など）も未決定。

### ナビゲーション

✅ **決定**：機能名ではなく**目的**でグループ分けします。

```
ホーム

知る
  AI音楽ニュース
  カラスイ Picks
  AIツール図鑑

つくる
  曲一覧
  プロンプト工房
  ストーリーボード
  制作ツール

マイスタジオ
  AI秘書 / データ管理 / プラン
```

- **PC**：左サイドバー（グループ見出し付き）— `src/app/Sidebar.tsx`
- **スマホ**：下部タブ5つ（ホーム／ニュース／つくる／学ぶ／マイ）— `src/app/TabBar.tsx`

スマホを下部タブにしたのは、毎日開くサービスは片手で操作できる必要があるためです。

---

## 3. ホーム画面仕様

### 表示順（✅ 決定・変更禁止）

実装：`src/features/home/HomePage.tsx`

| # | ブロック | 目的 | コンポーネント |
|---|---|---|---|
| ① | **曲を作る** | 制作の入口。**いちばん大きく別格に** | `CreateHero.tsx` |
| ② | 歌詞を書く | 制作の入口 | `CreateHero.tsx` |
| ③ | MVを作る | 制作の入口 | `CreateHero.tsx` |
| ④ | プロンプトを作る | 制作の入口 | `CreateHero.tsx` |
| ⑤ | **続きから** | 前回のつづきへ戻る | `ResumeSection.tsx` |
| ⑥ | 今日の重要ニュース（**3件だけ**） | 情報収集。主役ではない | `HomePage.tsx` 内 |
| ⑦ | **🔥 今日のカラスイ Picks（1件だけ）** | 制作者目線の解説 | `TodayPick.tsx` |
| ⑧ | おすすめプロンプト | ふたたび制作へ | `sections.tsx` の `PromptCard` |
| ⑨ | 制作ツール | 目的が決まっている人の近道 | `HomePage.tsx` 内 |
| ⑩ | 今日試すAIツール | 明日また来る理由 | `DailyTip.tsx` |

末尾に Substack の登録案内カードを1枚だけ置いています（`sections.tsx` の `SubstackCard`）。

**設計意図**：「制作 → 情報収集 → 学習 → ふたたび制作」の流れになるようにしています。

### ①〜④ 制作タイルの仕様

✅ **決定**：4つを同格にしない。「曲を作る」だけ別格に大きくします。
利用頻度が最も高くなる想定のためです。

| タイル | 遷移先 | 色 | 見た目 |
|---|---|---|---|
| 曲を作る | `/songs` | primary→suno グラデーション | **塗りつぶし・白文字・影あり・大** |
| 歌詞を書く | `/tools` | primary（枠線＋淡い背景） | 小 |
| MVを作る | `/storyboard` | mv（枠線＋淡い背景） | 小 |
| プロンプト | `/prompts` | premium（枠線＋淡い背景） | 小 |

- 色は用途ごとに固定します（毎日見ても迷わないため）
- 挨拶文は時間帯で変わります（朝／昼／夜）
- 右上に連続利用日数を小さく表示します

### ⑤ 続きから の仕様

✅ **決定**：履歴がまだ無い人には**セクションごと表示しません**（初回は入口だけを見せる）。

表示するもの：

1. **制作中の曲**（最終更新が最も新しい1曲）— 大きめのカードで「続きを作る」ボタン付き
2. **最近ひらいたもの**（最大8件・横スクロール）
3. **お気に入り**（最大8件・横スクロール）

記録するのは「何を開いたか」だけで、**作品の中身は保存しません**。

### ⑥ ニュースの見せ方

✅ **決定**：カードを大きくしない。**枠1つの中に3行**という控えめな見た目にします。

同じ3件でも、枠線1本でまとめて行の高さを下げるだけで「主役ではない」ことが伝わります。
情報量を減らすのではなく、**視覚的な重みだけを下げる**のがポイントです。

見出し：`今日の重要ニュース` ／ 補足：`3件・3分で読めます` ／ 右上：`もっと見る →`

### ⑦ カラスイ Picks の見せ方

✅ **決定**：**ホームは1本だけ**。一覧はPicksページに置きます（ホームで迷わせないため）。

```
🔥 今日のカラスイ Picks    今日の注目ニュースを制作者目線で解説    もっと見る →
┌────────────────────────────────────┐
│ [アイキャッチ]  今日の注目テーマ  2026年6月22日
│                大きなタイトル
│                冒頭300文字…
│                ［ 続きを読む → ］
│ ─── カラスイの制作メモ（Premium） ───
└────────────────────────────────────┘
```

ボタン文言は **「続きを読む」**。サービス名（Substack）はボタンに出しません。

### ⑩ 今日試すAIツール の仕様

✅ **決定**：`public/content/home.json` の `daily` から**1日1件だけ**選んで表示します。

選び方（`src/features/home/DailyTip.tsx` の `pickDailyTip()`）：

1. `date` が今日と一致する項目があれば、それを最優先で表示
2. 無ければ、`date` を持たない項目を**日替わりで巡回**

**この仕組みが重要な理由**：毎日書かなくても毎日変わるので、運営が止まりません。
Premiumの価値を「毎週コンテンツを配ること」に置かない方針と同じ考え方です。

現在は Suno Persona / Extend / Cover / Lyrics改善 / ステム分離 / Seedance / DomoAI など
8件が登録されており、何もしなくても8日サイクルで回ります。

---

## 4. プラン設計

### 3つのプラン

| | 無料 | Studio Premium | Studio Master |
|---|---|---|---|
| 種別 | ツール | **制作環境** | **学習環境（伴走）** |
| 目的 | まず触ってみる | 制作環境をアップグレードする | 本気で学び、成長する |
| 対象 | これから始める人 | 日常的に制作するクリエイター | 仕事・収益化につなげたい人 |
| スケール | 無限 | 無限 | **30名まで** |

✅ **決定**：Premium と Master は**上下関係ではなく別の目的**です。
「Premiumで足りない人がMaster」ではなく、
**「ひとりで作る人がPremium、見てもらって伸ばす人がMaster」**。

この並列関係を崩さないでください。上位互換として見せると、価格差が
「機能量の差」に見えてしまい、必ず割高に感じられます。

### 価格（✅ 決定・2026-08-02）

**Studio直販**（実装：`src/core/entitlement/role.ts` の `STUDIO_PRICE`）

| プラン | 価格 |
|---|---|
| Studio Premium | **980円 / 月** |
| Studio Master | **5,600円 / 月** |

**YouTubeメンバーシップ**（実装：同ファイルの `YOUTUBE_TIERS`）

| 段 | 価格 | 付与されるRole |
|---|---|---|
| AI大好き部 | 690円 / 月 | **なし**（応援用の段。Studioの権限は付かない） |
| Studio Premium利用権付き<br>（現「AIでMVを作りたい部」） | 1,290円 / 月 | `premium` |
| Studio Master利用権付き<br>（現「AIでMVを1か月でマスターしたい方向け」） | 5,600円 / 月 | `master` |

**Premiumだけ直販980円とYouTube1,290円で価格が違います。**
YouTube側にはメンバーシップ独自の特典が別途付くため、意図的な差です。
プラン画面では**Studio直販価格を大きく表示し、YouTube価格を小さく併記**します。

Masterの5,600円は現行のYouTube最上位メンバーシップと同額です。
既存メンバーとの公平性のため、この価格は下げません。

### 各プランの中身（✅ 決定）

**無料**
- AI音楽ニュース（全文・日本語訳つき）
- カラスイ Picks（冒頭）
- AIツール図鑑
- 制作ツール（歌詞レビュー・MVアイデア・AIプロデューサー）
- 曲の管理
- プロンプト工房（基本セット）
- **データの書き出し** ← ここは絶対に有料化しない

**Studio Premium**
- Premium限定ツール
- プロンプトライブラリ
- 制作テンプレート
- AI音楽ニュース詳細版
- カラスイ Picks 全文
- 制作履歴
- お気に入り
- 新機能先行公開

**Studio Master**（Premiumのすべて ＋ ここから先は人が関わる）
- 作品添削
- AI音楽レビュー
- Discord Master限定コミュニティ
- Master限定ライブ
- 優先サポート

### Premiumの価値モデル（✅ 決定・重要）

**「毎週コンテンツを配る定期便」ではありません。**
**「プロダクトが進化し続けること」に価値を置きます。**（Cursor / Notion 型）

| | 定期便モデル（**不採用**） | 進化する道具モデル（**採用**） |
|---|---|---|
| 価値の源泉 | 毎週配るコンテンツ | プロダクトそのものが良くなること |
| 継続理由 | 「今週も何か届く」 | 「手放すと制作が遅くなる」 |
| 運営負荷 | 毎週締切がある | 締切がない。積み上げでよい |
| 破綻条件 | 1週休むと解約が始まる | 数か月止まらない限り問題ない |

**なぜ定期便をやめたか**：毎週コンテンツを作る運営は一人では破綻します。
そして破綻した瞬間に「今週は何も来なかった」という解約理由が生まれます。
つまり定期便モデルは、リスクを減らすどころか新しい失敗ポイントを増やしていました。

Premiumで売るのは次の4つです。

1. Studioが便利になり続ける（新機能・改善が毎月入る）
2. 制作時間が短くなる（テンプレート・ワークフロー）
3. AI音楽ニュースが毎日更新される（**すでに全自動**）
4. ツールが増えていく

このうち3つは、すでに自動化されているか通常の開発で自然に積み上がります。
「Premiumのために追加でやる作業」がほぼ発生しないのが、定期便モデルとの決定的な差です。

### Master の人数上限（✅ 決定）

**30名まで。** 上限がないと添削が破綻し、「反応が遅い」という
最悪の解約理由を生みます。UIでは常時「少人数制（30名まで）」と表示します。

### Version 1 ユーザーへの配慮（✅ 決定・実施必須）

制作履歴とお気に入りをPremiumに含めた判断とセットで、次を必ず実施します。

1. **V1利用者がV2へ移行する際、Premiumを1年間無償で付与する**
   （期間は「永年」ではなく「1年間」。その後は運営状況を見て判断）
2. **データの書き出しは無料のまま維持する**

これを守らないと、V1で無料だった機能が有料になったと受け取られます。

---

## 5. 権限設計（Role）

### 原則

✅ **決定**：**権限は価格ではなく Role で管理します。**

```
free / premium / master
```

入手経路が違っても、最終的に同じ Role になります。
**画面側は「いくら払ったか」「どこで買ったか」を一切見ません。**

| 入手経路 | Role |
|---|---|
| YouTube「Studio Premium利用権付き」 1,290円 | `premium` |
| Studio Premium 直販 980円 | `premium` |
| YouTube「Studio Master利用権付き」 5,600円 | `master` |
| Studio Master 直販 5,600円 | `master` |
| YouTube「AI大好き部」 690円 | （権限なし・`free` のまま） |

### 実装

**唯一の実装場所：`src/core/entitlement/role.ts`**

```ts
export type Role = 'free' | 'premium' | 'master'
export type RoleSource = 'none' | 'youtube' | 'studio'

// 画面側はこれだけを使う
export function useRole() {
  return { role, source, paid, master, label }
}
```

| エクスポート | 用途 |
|---|---|
| `useRole()` | **画面側は必ずこれを使う。** 直接ストアを触らない |
| `isPaid(role)` | Premium以上か |
| `isMaster(role)` | Masterか |
| `STUDIO_PRICE` | Studio直販価格 |
| `YOUTUBE_TIERS` | YouTubeメンバーシップとRoleの対応 |
| `DISCORD_INVITE_URL` | Master限定Discordの招待URL |

保存先（localStorage）：
- `studio-next:role` … Role
- `studio-next:role-source` … 入手経路

### 認証設計

✅ **決定**：Studioはアカウントもサーバーも持ちません。
**ユーザーがStudioで別途登録し直す必要はありません。**

```
YouTubeメンバーシップに加入
    ↓
加入プランに応じたキーが届く
    ↓
Studioのプラン画面に貼る
    ↓
端末内で検証してRoleを付与
```

キーは端末内で確認され、外部には送られません。

🚧 **未実装**：現在は検証用の仮実装です。
`src/features/plans/PlansPage.tsx` の `MembershipBox()` の中で、
キー文字列に `PREMIUM` / `MASTER` が含まれるかを見ているだけです。

**正式な署名検証を実装するときは、この関数の中だけを差し替えてください。**
`RoleSource` で入手経路も記録しているので、将来サーバー方式へ移行しても
画面側の書き換えは不要です。

### 検証用の権限切替

開発中は画面上部に権限切替バーが出ます（`src/app/DevRoleSwitcher.tsx`）。
本番ビルドでは自動的に非表示になります（`import.meta.env.PROD` で判定）。

正式なキー検証を実装したら、このファイルごと削除してください。

---

## 6. Discord設計

| 項目 | 値 |
|---|---|
| コミュニティ名 | **AI音楽部 Lab** |
| 対象 | **Master限定** |
| 招待URL | `https://discord.gg/zdGrkph8` |
| 実装 | `src/core/entitlement/role.ts` の `DISCORD_INVITE_URL` |

✅ **決定**：**Master のときだけ**案内します。

プラン画面のMasterカードに「Discordコミュニティへ参加」ボタンを表示します。
`free` / `premium` では表示しません（実装確認済み）。

役割は**交流**です。Studioの中に交流機能は作りません。

---

## 7. Substack連携（カラスイ Picks）

### 取得方法

✅ **決定**：**RSS**で取得します。APIは不要でした。

| 項目 | 値 |
|---|---|
| フィードURL | `https://karasui.substack.com/feed` |
| 収集スクリプト | `scripts/collect-picks.mjs` |
| 出力先 | `public/content/picks.json` |
| 実行 | `npm run picks` |
| 自動更新 | GitHub Actions が1日4回 |

**注意**：プロフィールURL `substack.com/@karasui` にRSSはありません。
刊行物のサブドメイン（`karasui.substack.com`）が正しい取得先です。

別の刊行物を使う場合は環境変数 `SUBSTACK_FEED` で切り替えられます。

### 取得する項目

| 項目 | 取得元 |
|---|---|
| タイトル | `<title>` |
| リンク | `<link>` |
| 日付 | `<pubDate>` |
| アイキャッチ | `<enclosure>`（画像のみ。**ポッドキャスト回の音声URLは除外**） |
| 冒頭 | `<content:encoded>` の先頭**300文字** |

### 実装上の注意（ハマりどころ）

1. **Substackが本文に差し込む購読案内を除去すること。**
   除去しないと、記事の冒頭のはずが「ぜひ無料で購読してください」で埋まります。
   `removeSubscribeCta()` で処理しています。

2. **`<enclosure>` は画像とは限りません。**
   ポッドキャスト回では音声URLが入るため、画像判定してから使います。

3. **記事の紐付けキーは記事URLの `/p/` 以降**（例：`aiip`）。

### 表示ルール

✅ **決定**

| 場所 | 表示 |
|---|---|
| ホーム | **最新1件だけ**（`TodayPick.tsx`） |
| Picksページ | **全件**（`PicksPage.tsx`） |

### Studioに本文を載せない理由（✅ 決定・重要）

**冒頭300文字までで、全文はSubstackで読んでもらいます。**

Studioに全文を写すと、Studioだけが育ってSubstackの読者が増えません。
読者数はSubstackの資産なので、そこは削るべきではありません。
「一つ記事を書くたびに3つが同時に育つ」という狙いを守るための判断です。

### Premiumの価値の置き場所

**「本文の続き」ではなく「制作メモ」に置きます。**

記事ごとに、Studio限定の制作メモを `public/content/home.json` の `pickNotes` に
手で書きます。キーは記事URLの `/p/` 以降です。

```json
"pickNotes": {
  "aiip": {
    "note": "この記事のIPの考え方は、Studioの曲管理と相性が良いです。…",
    "prompts": ["p-lofi-drive"]
  }
}
```

これなら無料の人もPremiumの人もSubstackを読みに行きます。
そのうえでPremiumだけが「自分の制作にどう使うか」を受け取れます。

`npm run picks` を実行すると、**制作メモがまだ無い記事を教えてくれます。**

---

## 8. AI音楽ニュース仕様

### 基本方針

✅ **決定**：**制作の邪魔をしないこと。**

| 場所 | 表示 |
|---|---|
| ホーム | **3件だけ**（控えめな見た目） |
| ニュースページ | 全件＋絞り込み＋検索＋注目ランキング |

### 収集

| 項目 | 値 |
|---|---|
| 情報源 | **28フィード**（`scripts/feeds.json`） |
| 収集スクリプト | `scripts/collect-news.mjs`（外部ライブラリ0） |
| 出力先 | `public/content/news.json` |
| 実行 | `npm run news` |
| 自動更新 | 日本時間 **6:00 / 12:00 / 18:00 / 22:00** |

### 記事の絞り込み（媒体の性質ごとに基準を変える）

✅ **決定**：全部同じ基準にすると精度が落ちます。

| `filter` 値 | 条件 | 向いている媒体 |
|---|---|---|
| `all` | 全部通す | 検索条件自体が絞り込みになっているフィード |
| `ai` | 見出しにAI関連の語 | 音楽専門メディア |
| `music` | 見出しに音楽関連の語 | AI専門メディア |
| `keyword` | 見出しにAIと音楽の語が**両方** | 総合ニュースサイト |

どの場合も「Suno」「音楽生成」などの決定的な語が本文にあれば無条件で採用します。

### 実装上の注意（ハマりどころ）

1. **英数字の語には単語境界 `\b` が必須。**
   これが無いと `udio` が st**udio** / a**udio** に誤爆します（実際に起きました）。

2. **日本語ニュースの網羅には Google News RSS が要です。**
   日本の一般メディアの通常RSSでは、AI音楽記事はほぼ拾えません。

3. **同一記事の重複は名寄せすること。**
   「（1/2枚目）」「(ITmedia NEWS)」のような接尾辞を落として比較します。

### 海外ニュースの日本語化

✅ **決定**：見出しと要約を自動翻訳します。表示は**日本語が主、原文を小さく併記**。

| | |
|---|---|
| 実装 | `scripts/translate.mjs` |
| 主 | MyMemory（公式API・無料枠1日5,000語） |
| 予備 | Google翻訳の非公式エンドポイント |
| APIキー | **不要** |

- 片方が止まっても、もう片方に自動で切り替わります
- **一度訳した記事は記録され、二度と翻訳し直しません**（1回の実行で最大70件）
- 環境変数 `MYMEMORY_EMAIL` を設定すると上限が1日50,000語に増えます（任意）

⚠️ **注意**：これは**機械翻訳**です。配信元の要約を翻訳したもので、
AIが新たに書き起こした要約ではありません。フッターにもその旨を明記しています。

### 注目ランキング

✅ **決定**：ニュースページに**上位5件**を表示します。

「AI音楽をこれから始める人が知りたいか」を基準にスコアリングします。

| 加点 | 点 |
|---|---|
| 使い方・始め方・入門 | +14 |
| 無料で試せる | +12 |
| 日本語対応 | +10 |
| 新機能・リリース | +11 |
| Suno / Lyria 等のサービス名 | +9 |
| 著作権・商用利用 | +7 |
| **複数媒体が報道** | **最大 +24** |

| 減点 | 点 |
|---|---|
| 買収・資金調達・決算 | −8 |
| 機材レビュー | −7 |
| 1週間より古い | −6 |

**1話題につき1枠だけ**載せます（同じ話題で5枠が埋まるのを防ぐため）。
同話題の判定は見出しの文字2つ組の重なり（閾値 **0.13**）と固有名詞の共通部分で行います。
実測値：同話題の組 = 0.19〜0.28 ／ 無関係な組 = 0.04 以下。

---

## 9. データ保存

### 原則

✅ **決定**：**すべて端末内（localStorage）にのみ保存し、外部へは送りません。**

アカウント登録もログインもありません。

### 保存しているもの

| 内容 | キー | 実装 |
|---|---|---|
| 曲データ | `studio-next:songs:v1` | `src/core/storage/songs.ts` |
| 最近ひらいたもの | `studio.recent` | `src/core/storage/activity.ts` |
| お気に入り | `studio.favorites` | `src/core/storage/activity.ts` |
| 連続利用日数 | `studio.visits` | `src/core/storage/streak.ts` |
| 権限 | `studio-next:role` | `src/core/entitlement/role.ts` |
| 入手経路 | `studio-next:role-source` | `src/core/entitlement/role.ts` |
| 表示テーマ | `studio.theme` | `src/core/ui/useTheme.ts` |
| Substack案内を閉じた日 | `studio.substackDismissed` | `src/features/home/sections.tsx` |
| プロンプトのお気に入り | `studio-next:prompt-dex:favorites:v1` | `src/features/tools/prompt-dex/lib/` |

### 「続きから」で記録するもの

記録するのは**「何を開いたか」だけ**です。作品の中身は保存しません。

```ts
interface Activity {
  id: string
  kind: 'song' | 'prompt' | 'tool' | 'external'
  label: string
  to: string | null      // Studio内の遷移先
  url: string | null     // 外部ツールのURL
  at: number             // 最後に触った時刻
}
```

最大20件まで保持します。

### 曲データ

```ts
interface Song {
  id, title, status, genre, mood,
  lyrics, sunoPrompt, mvPrompt, memo,
  createdAt, updatedAt
}

type SongStatus = 'idea' | 'lyrics' | 'suno' | 'mv' | 'published'
```

⚠️ **注意**：V1の `SongStatus` は `'published'` です（`'done'` ではありません）。
V1からロジックを移植する際にここを間違えると型エラーになります。

曲詳細では**入力が止まってから600ms後に自動保存**します
（保存ボタンを押し忘れて消える事故をなくすため）。

### データの持ち出し

✅ **決定**：`/data` から JSON で書き出し・読み込みできます。**この機能は無料のまま維持します。**

読み込みは既存データを消さず、同じIDだけ上書きします。

---

## 10. アーキテクチャ

### Version 2 最大の設計変更：アプリとコンテンツの分離

```
src/                      アプリ（滅多に変わらない）
├── app/                  ルーティング・シェル
│   ├── AppShell.tsx      全体レイアウト
│   ├── Sidebar.tsx       PC用サイドバー
│   ├── TabBar.tsx        スマホ用下部タブ
│   ├── Topbar.tsx        上部バー
│   ├── DevRoleSwitcher   検証用の権限切替（本番では非表示）
│   └── navigation.ts     ナビゲーション定義
├── core/                 全機能が使う土台
│   ├── content/          コンテンツ取得・検証（load.ts / types.ts）
│   ├── entitlement/      権限判定（role.ts）
│   ├── storage/          songs / activity / streak
│   └── ui/               cn.ts / useTheme.ts
└── features/             機能ごとに縦割り
    ├── home/ news/ picks/ songs/ tools/ tools-guide/ data/ plans/

public/content/           コンテンツ（毎日変わる）
├── news.json             GitHub Actions が1日4回更新
├── picks.json            GitHub Actions が1日4回更新（Substack）
└── home.json             手で書く（今日のツール・プロンプト・図鑑・制作メモ）

scripts/
├── collect-news.mjs      ニュース収集
├── collect-picks.mjs     Substack記事の取り込み
├── translate.mjs         英日翻訳
└── feeds.json            28フィードの設定
```

**この分離の利点**：

- **コンテンツ更新にアプリのビルドが要りません**（＝毎日更新できる）
- 機能追加は `features/` にフォルダを1つ足すだけ（＝3〜5年スケールする）

### 技術スタック

| | |
|---|---|
| フレームワーク | React 19 + TypeScript |
| ビルド | Vite 8 |
| スタイル | Tailwind CSS 3 |
| 状態管理 | Zustand |
| ルーティング | react-router-dom（**HashRouter**） |
| アイコン | lucide-react |
| テスト | Vitest（**141件**） |

**HashRouterを使う理由**：GitHub Pagesのような静的ホスティングで動かすためです。

**`base: './'`**：サブパス公開に対応するため相対パスで出力します。

### パフォーマンス

制作ツールは `lazy()` で遅延読み込みします。ホームのJSは **約98KB（gzip）** に保っています。

「毎日開きたくなるホーム」を目指す以上、ホームの表示速度は落とせません。

### V1から移植したロジック

**V1のテスト141件が、V2でもそのまま全部通っています。ロジックは1行も書き換えていません。**

移植の手順（今後ツールを増やすときも同じ）：

1. `lib/` をコピー
2. `import` を `deps.ts`（必要な型だけ写した薄いファイル）に付け替え
3. **テストを走らせて、通ることを確認**
4. UIだけ新規に作る

3を先にやることで、UIを書く前に「ロジックが壊れていない」と確定できます。

⚠️ `ai-producer/lib/compare.ts` は実行履歴機能に依存するため**持ち込んでいません**。
履歴機能を作るときに一緒に入れてください。

### 自動化（GitHub Actions）

| ワークフロー | 内容 | タイミング |
|---|---|---|
| `deploy.yml` | ビルドしてGitHub Pagesへ公開 | コードをpushしたとき |
| `update-content.yml` | ニュース・Picksの収集 | 日本時間 6/12/18/22時 |

2つに分けているので、**ニュースが更新されるたびにアプリを作り直す必要はありません**。

---

## 11. ロードマップ

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | プロジェクト基盤・デザインシステム・シェル | ✅ 完了 |
| 2 | コンテンツ基盤＋ニュース機能 | ✅ 完了 |
| 3 | トップページ（制作ファースト） | ✅ 完了 |
| 4 | 制作ツール4種をV1から移植 | ✅ 完了 |
| 5 | 曲一覧・データ管理・Picks・ツール図鑑・プラン画面 | ✅ 完了 |
| 6 | **GitHub Pagesへ公開** | 🚧 **次にやること** |
| 7 | ライセンスキーの署名検証・決済導入 | 🚧 未実装 |
| 8 | V1からの移行（データ移行・切替） | 🚧 未実装 |

### Phase 6：公開手順（次にやること）

**GitHubのWeb画面から操作します。ターミナルのpushは使いません**（認証が通らないため）。

1. GitHubで新規リポジトリ `studio-next` を作成（**Public**）
2. フォルダの中身をドラッグ＆ドロップ（**`node_modules` と `dist` は含めない**）
3. `.github/workflows/deploy.yml` と `update-content.yml` をWeb画面で個別に作成
   （隠しフォルダなのでドラッグでは上がりません）
4. **Settings → Actions → General → Workflow permissions を「Read and write」に**
5. **Settings → Pages → Source を「GitHub Actions」に**
6. Actionsタブから2つのワークフローを一度ずつ手動実行

詳しい手順は `README.md` の「公開のしかた」に記載しています。

### Phase 7 以降でやること

- ライセンスキーの署名検証（`PlansPage.tsx` の `MembershipBox()` を差し替え）
- 決済導入（Stripe など）とキー発行の運用フロー
- Premium限定ツールの中身を決めて実装
- 制作テンプレートの実装

---

## 12. 未決定事項

❓ **勝手に決めずに、必ず確認してください。**

| # | 項目 | 補足 |
|---|---|---|
| 1 | **YouTubeチャンネルのURL** | プラン画面の「YouTubeで加入する」ボタンが `https://www.youtube.com/` のままです。メンバーシップページのURLに差し替えが必要 |
| 2 | **マイページを作るか** | 現在は「マイスタジオ」グループで代用。独立ページを作る場合、何を載せるかも未定 |
| 3 | **AI秘書をどうするか** | プレースホルダのまま。制作ファースト設計では「今日試すAIツール」が役割を担っているため、そもそも必要かの判断から |
| 4 | **独自ドメイン** | `studio.○○○` の取得。当面は GitHub Pages のURLで運用 |
| 5 | **決済手段** | Stripe導入の時期とキー発行の運用フロー |
| 6 | **Premium限定ツールの中身** | 「Premium限定ツール」として何を作るかが未定 |
| 7 | **制作テンプレートの中身** | 同上 |
| 8 | **V1からのデータ移行方式** | 最低限でよい。将来対応で十分という合意 |
| 9 | **Master 30名に達したときの運用** | 待機リストを作るか、募集を止めるか |
| 10 | **ニュースの「詳細版」の中身** | Premium特典の「AI音楽ニュース詳細版」が具体的に何かが未定 |

---

## 13. 用語集

| 用語 | 意味 |
|---|---|
| **V1 / Version 1** | 既存Studio（`~/AI Music Studio`）。本番稼働中。**触らない** |
| **V2 / Version 2** | このプロジェクト（`~/AI音楽部 Studio Next`） |
| **Role** | 権限。`free` / `premium` / `master` |
| **RoleSource** | 権限の入手経路。`none` / `youtube` / `studio` |
| **カラスイ Picks** | Substackの記事をStudioに表示する機能。旧称「AI音楽部 Picks」 |
| **制作メモ** | Picks記事ごとのStudio限定の補足。Premiumの価値の中心 |
| **続きから** | 前回さわったものへ戻るホームのブロック |
| **AI音楽部 Lab** | Master限定のDiscordコミュニティ |

---

## 14. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-08-02 | 仕様書を新規作成。価格を確定（Studio直販 Premium 980円 / YouTube 1,290円の併存、AI大好き部690円を追加） |
| 2026-08-01 | 権限をRoleベースに再実装。ホームを制作ファーストで全面再設計。Substack連携を実装。制作ツール4種の移植完了 |
| 2026-07-31 | プロジェクト開始。設計方針を策定 |
